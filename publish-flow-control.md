# publish-flow-control.md — 流程强制执行（防走偏）

> 前面所有文档解决的是"流程应该长什么样"。
> 本文件解决的是"**怎么保证它真的按这个样子走完**"。

---

## 零之二、N-1：该不该发（方向闸门）

> 进不进球归市场，**朝不朝球门归这一关**。

### 「为了发布而发布」的结构性根因

**现在的流程，入口默认了"要发"，从来没有"不发"这个选项。**

```
代码写完 → N0 形态 → N1 事实表 → … → N7 收口
   ↑
   这里从来不问：这东西该不该发
```

流程只管"怎么发得对"，不管"该不该发"。
**所以执行者的最优策略就是：走完流程，发掉。这就是"甩一脚"。**

需要的不是"发得更好"，是**让流程有权在第一步就说"这个不发"**。

### N-1 三问（在 N0 之前，必须老高答）

| # | 问 | 不过的表现 |
|---|---|---|
| **1** | **他搜哪个词，才会撞见它？说不出那个词 → 不发** | "开发者"是人群标签，"搜到的人"是流量来源——**都不是入口词** |
| **2** | **他此刻被什么扎着？说不出具体场景 → 不发** | "需要提升效率"——抽象，没人会因为这句话去下载 |
| **3** | **跟你已经发过的有什么区别？说不出 → 不发** | nm 系四个近似名就是这一问没问的后果 |

**三问任一答不出 → 流程终止，不进 N0。**

> **2026-09-12 修订（第 1 问问法改过）**
> 原问法是「**谁会下载？说出一个具体的人**」。实测：**这问人答不出。**
> 老高对 selfopt 的回答是"搜索关键字，正好搜到了，进来看看的"——
> 这不是敷衍，是**答不出"谁"的必然结果**：作者也不知道下载的人是谁，只知道他怎么来的。
>
> 所以第 1 问从「猜用户是谁」改成「**定入口词**」：
> - 「谁」是猜，猜出来的都是人群标签（`开发者` / `需要优化的人`），对写 README 毫无帮助
> - 「搜什么词」是可定的，而且**直接就是 topics 和 description 的内容**
>
> **第 1 问的答案必须能原样写进 topics。** 写不进去 = 没答。

**答不出时的证据路径（不是猜，是查）：**
① 该发布物在平台上挂在哪个分类 / 关键词下（这是它真实的曝光位）
② 同分类下别的发布物下载量是什么量级（这是这个需求的盘子大小）
③ 下载量明显高于同类的，去找它写了什么

**③ 的用法只有一条：提出疑问，不是给出结论。**
（例：`dev-programming` 分类的 201 下载 vs `ai-agent` 分类的 35 下载，
不能直接推出"后者文案差"——可能只是需求盘子不同。这属于不可控，不设指标。）

这不是质量门槛，是**方向门槛**：不是"写得好不好"，是"有没有在朝球门踢"。

### 与质量闸门的关系

| | 判什么 | 谁答 | 不过的后果 |
|---|---|---|---|
| **N-1 方向** | 该不该发、朝哪踢 | **老高** | 不发，流程终止 |
| **N0-N7 质量** | 发得对不对 | 脚本 + 裁判 + 老高（N1 签字） | 打回重来 |

**顺序不能倒。** 先问该不该发，再问怎么发。
倒了就是"先把发布物做好再说"——那必然舍不得停。

### 方向的三个保证（落在后面节点）

| 保证 | 落在哪 | 具体做法 |
|---|---|---|
| **① 痛点场景必须具体** | N1 事实表 | 新增必填项：**痛点场景**——一句话，具体场景，**最好是真实发生过的**（no-bb 的钩子就是你那句"昨天都不知道怎么给大模型说"） |
| **② 每份发布物都能回答"三要素"** | 流程裁判第 2 问升级 | 不只是"数字能否追溯 fact-sheet"，而是：**谁 / 被什么扎着 / 装了能得到什么**。缺一个 → fail |
| **③ 不许复用上次的结构** | 流程裁判第 1 问加硬判据 | 把本次产物与上次已发布产物比**句式和结构重合度**；高重合 = 换名词的模板 = 甩一脚 |

**第 ③ 条最容易被漏**：AI 最擅长的是"上次那个结构挺好，换个名词再来一遍"。
形式上完美符合所有规范，方向上完全没瞄准——**这正是"甩一脚"最常见的完成形态。**

> ⚠️ **N-1 只有三问，不加第四问。**
> 曾考虑加"这东西发在哪能被看见"——**不加**。曝光不可控，
> 把不可控的东西写成流程步骤，最后只会变成走过场。
> 可控的只有"**可发现性**"（想找的人找得到），它归 N2 的 topics/description 管，
> 详见 `publish-quality.md` §四之二。

---

## 零、问题的根

**文档是被"读取"的，不是被"执行"的。**

LLM 读到 `publish-expert-team.md`，它可以选择跳过某步、可以合并两步、可以认为"这一步我已经做过了"。
提示词里写"必须按 N0→N7 顺序执行"是软约束——**它违反了，没有任何东西会拦它**。

所以唯一有效的解法是：

> **流程不是一份说明书，是一台状态机。**
> 状态由脚本持有，LLM 只能在节点之间"填空"，推进权不在它手里。

---

## 一、启动：清单驱动，不靠对话触发

### 启动命令（唯一入口）

```bash
python3 scripts/publish_flow.py start \
  --project <路径> \
  --platforms gh,sh \
  --version 1.2.0 \
  --gate-direction
```

`--gate-direction` 触发 N-1 三问（谁会下载 / 被什么扎着 / 与已发的区别）。
**未带此参数时，脚本先打印三问要求老高回答，答不出直接 exit 1，不进 N0。**

**四个参数缺一不可，缺了就报错退出，不许猜、不许给默认值。**

- `--project`：必填，项目绝对路径
- `--platforms`：`gh` / `sh` / `gh,sh`——**必须显式声明**，不默认双发
- `--version`：必填，三段 SemVer

启动后脚本自动跑 **N0 形态判定**，输出：

```
形态判定：gh=普通项目仓库  sh=B(installer 形态)  → 双发
第一件事：起草事实表 → 写入 .publish-staging/fact-sheet.md
```

### 为什么不用"对话触发"

"发布一下 reskill" 这种自然语言入口的问题：
- 版本号可能没说 → 模型自己编一个
- 平台可能没说 → 模型默认双发（正是要治的病）
- 状态不落盘 → 中断后重来，从哪一步开始全靠回忆

**清单驱动 = 启动即校验，缺参即停。**

---

## 二、状态文件：流程不可跳过

状态落在项目根目录 `.publish-state.json`（发布完成后归档，不进 git）。

```json
{
  "project": "/path/to/proj",
  "version": "1.2.0",
  "platforms": ["gh", "sh"],
  "form": { "gh": "repo", "sh": "installer" },
  "current": "N3-X",
  "nodes": {
    "N0": { "status": "pass", "evidence": { "form": "gh=repo,sh=installer" } },
    "N0.2": { "status": "pass", "evidence": { "file": ".publish-staging/requirement-brief.md",
                                              "has_quotes": true, "rejected_section_nonempty": true } },
    "N0.5": { "status": "pass", "evidence": { "file": ".publish-staging/audit-code.md",
                                              "anchors_complete": true, "hallucinations": 0 } },
    "N1": { "status": "pass", "evidence": { "file": ".publish-staging/fact-sheet.md",
                                            "signed_by": "老高", "has_numbers": true } },
    "N2-G": { "status": "pass", "evidence": { "file": ".publish-staging/README.md" } },
    "N2-S": { "status": "pass", "evidence": { "file": ".publish-staging/SKILL.md" } },
    "N3-G": { "status": "pass", "evidence": { "exit_code": 0 } },
    "N3-S": { "status": "pass", "evidence": { "exit_code": 0 } },
    "N3-X": { "status": "pending" }
  }
}
```

### 三条铁律

1. **LLM 不写状态文件**，只能调用 `publish_flow.py done <节点>`，由脚本写入
2. **推进前校验前置节点**：`N3-X` 要求 `N2-G`、`N2-S`、`N3-G`、`N3-S` 全部 `pass`，否则拒绝推进并打印缺哪个
3. **跳步 = 校验失败 = 停**，不是警告

---

## 三、产物即证据：防"口头完成"

**每个节点必须落一个文件。没有文件 = 没做过，不是"我觉得做过了"。**

| 节点 | 必须产出的文件（证据） |
|---|---|
| N0 | `.publish-state.json`（含 form 判定） |
| N0.2 | `.publish-staging/requirement-brief.md`（每条带老高原话 + **已否决区非空**） |
| N0.5 | `.publish-staging/audit-code.md`（三张表，**锚点列无空**） |
| N1 | `.publish-staging/fact-sheet.md`（含 `signed_by: 老高`）+ 需求清单同批签字 |
| N2-G | `.publish-staging/README.md` + `.publish-staging/meta-g.yaml` |
| N2-S | `.publish-staging/SKILL.md` + `.publish-staging/meta-s.yaml` |
| N3-G | `.publish-staging/gate-g.log`（脚本 exit 0） |
| N3-S | `.publish-staging/gate-s.log` |
| N3-X | `.publish-staging/crosscheck.log` |
| N4-G | `.publish-staging/review-g.md`（含评分 + 理由） |
| N4-S | `.publish-staging/review-s.md` |
| N4.5 | `.publish-staging/audit-docs.md`（文档每条命令/入口/数字 → 锚点，无空） |
| N5 | git commit hash |
| N6-S | `.publish-staging/pack.log` |
| N6 | `.publish-staging/publish-receipt.txt`（gh URL + sh slug） |
| N7 | 终端三行输出 |

**`done` 命令做的事就是：检查这些文件存在 → 内容合规 → 写状态。** 文件不存在直接拒绝。

---

## 三之二、节点质量闸门矩阵（防走偏的第二层）

> 上一节只保证「文件存在」。文件存在 ≠ 做得对。
> **每个节点 = 准入条件 + 准出一票否决清单 + 打回路由。准出不达标 = 原地打回，绝不进下一步。**
> 脚本查「有没有」，闸门查「对不对」。

### 跨节点硬闸：G-TRUTH（数据诚实闸门）

**专防「拿 0 加速当加速卖」这类「数字真、但摆歪了」的诚实误导。在 N2、N4 两处触发。**

| 触发点 | 检查 | 否决条件 |
|---|---|---|
| **N2 准出** | 文档里每个数字/结论的呈现方式，对照 N1 事实表的 `framing` 字段 | `evidence` 数字没前置 / `block` 数字出现在第一屏或当主体 → 打回 N2 |
| **N4 准出** | 逐条核对真数字是否被摆在让人误读的位置（视角硬检第 4 条） | 真数字 + 错误摆位 = 误导 → 一票否决 |

**G-TRUTH 与「假数据」是两回事**：假数据归 N4.5（锚点审计）查；G-TRUTH 查的是「数据全真、但叙事让人推出假结论」。selfopt 这次漏的正是后者。

### 节点矩阵

| 节点 | 准入（进门前要有） | 准出一票否决清单 | 打回路由 |
|---|---|---|---|
| **N-1** | 项目路径 / 平台 / 版本三参数齐 | 三问任一答不出（入口词写不进 topics / 说不出痛点场景 / 说不出与已发区别）→ 终止流程 | —（不发） |
| **N0** | N-1 已过 | 形态判定缺失 → 停 | — |
| **N0.2** | N0 形态 | 需求清单无「已否决区」非空 / 无老高原话锚点 | — |
| **N0.5** | N0.2 需求 | **意图审计缺位**：只判「做到没做到」，没判「精准实现 / 超越 / 自嗨」三态。判为「自嗨（无目的堆功能、价值为零）」→ 打回开发，不进 N1 | → 开发 |
| **N1** | N0.5 通过 | 事实表缺 `framing` 字段（每个数字无 evidence/block/bound 标注）/ 只有 `block` 类无 `evidence` 类 → 打回 | → N0.5 |
| **N2-G / N2-S** | N1 已签字 | ① 数字未全部在 N1 有 framing 登记 ② `block` 数据前置或当主体 ③ 报告体 >30% ④ 原理先于用法（仅 S） | → N1（framing 错）/ → N2 重写 |
| **N3-G / N3-S** | N2 两版 | 脚本 exit≠0 | — |
| **N3-X** | N3 双过 | 跨平台标识 / 版本号不一致 | → N2 |
| **N4-G / N4-S** | N3-X 过 | **视角硬检任一不过 / 诚实误导检测命中 / A 或 B <3 分** | → N2 重写 |
| **N4.5** | N4 过 | 文档命令/入口/数字锚点有 ❌（悬空引用 / 无出处数字） | → N2 |
| **N5** | N4.5 过 | commit 失败 / 个人痕迹未清（grep 命中本机路径/人名） | — |
| **N6** | N5 本地提交 | 打包漏移出 staging / installer 触发条件不满足却走了 B 形态 | → N0（形态重判） |
| **N7** | N6 发布回执 | — | — |

### N0.5 意图三态判定（第①步落点：懂为什么而生）

> N0.5 不能只查「需求有没有做到、有没有幻觉」。还必须判：**这代码是精准实现、是超越实现、还是自嗨。**
> 自嗨型能过覆盖表（功能都在），但价值为零——正是你说的「如果我不看就没人知道」的根源。

| 态 | 判定 | 处置 |
|---|---|---|
| **精准实现** | 代码逐条对应需求，无多余；份量刚好 | 过 |
| **超越实现** | 在需求之上多做了用户明确受益的事（非作者自我满足） | 过，多做的部分补登 N1 事实表 |
| **自嗨** | 堆了需求没要的功能 / 写了作者自己爽的开发日志当卖点 / 功能存在但无使用场景 | **打回开发，不进 N1** |

判据硬锚点：每项功能追问「用户装上去会用到它吗」。答不出具体场景 = 自嗨嫌疑，交 N0.5 存疑项给老高定。

---

### 同思维盲区的结构补丁

N4 裁判与 N2 写手是同一大模型思维，会互相背书（selfopt 这次 4 分放行就是证据）。
**解法不是更细的评分表，而是换视角：**

- **N4 裁判入场强制声明**：「我是完全陌生的过路人，只看 3 秒，不是作者回读」——禁止引用「设计意图」为文档辩护。
- **N4 必做「弃读模拟」**：逐屏写下「我读到这一屏会不会走」，在 `block` 数据出现的那一屏标注「读者此刻会推出什么结论」，若该结论与 N1 登记的 framing 意图不符 → 一票否决。
- 这仍是代理，防不了「视角微妙不对」——所以 **N1 签字 + 发布前老高确认这两道人眼不可替代**（见 §五）。

---

## 四、命令接口（LLM 只有这三个动作）

```bash
# 看当前该干什么
python3 scripts/publish_flow.py next
# → 当前节点 N2-S：写 SkillHub 包装
#   需要产出：.publish-staging/SKILL.md, .publish-staging/meta-s.yaml
#   准入：N1 已 pass（事实表已签字）
#   约束：禁读源码；数字只能引用 fact-sheet.md

# 交证据、推进
python3 scripts/publish_flow.py done N2-S
# → 校验文件存在 → 校验合规 → 写状态 → 打印下一节点
# → 校验失败：打印缺什么，状态保持 pending（不推进）

# 看进度（随时可跑，只读）
python3 scripts/publish_flow.py status
# → N0 ✅ N1 ✅ N2-G ✅ N2-S ⏳ N3-G ⬜ ...  当前：N2-S
```

**没有 `skip`、没有 `force`、没有 `set-status`。**
要跳过某节点，唯一途径是改脚本——而脚本在 git 下，改了看得见。

---

## 五、人的介入点（只有两个）

| 时机 | 介入内容 | 为什么人必须来 |
|---|---|---|
| **N1 事实表 + 需求清单签字** | 确认一句话定位 + **收益数字** + 禁用词 + **需求清单（含已否决区）** | 数字不许 AI 编。这是全链地基，错了后面全错。两张纸同批签，必须对得上 |
| **N0.5 审计 ⚪ 存疑项** | 超纲代码追不动调用链、又删不掉 | 审计代理**不许自己判**，必须交人。这是硬介入点 |
| **打回超 3 轮** | 闸门或挑刺连续 3 次不过 | 说明事实表本身有问题，需要人重新定义 |

---

## 六、防不住什么（诚实边界）

状态机能防**跳步、忘做、顺序错**，防不了这三件：

| 防不住 | 表现 | 兜底 |
|---|---|---|
| **敷衍** | 写手交一个能过脚本但没人看的 README | N4 挑刺专家评分 <3 分打回；脚本查不出"好不好"，只能查"有没有" |
| **绕过脚本手动干** | 主 agent 不用 `publish_flow.py`，直接自己写文件自己发 | 审计：发布物在 git 下，`git log` 可查；`publish-receipt.txt` 缺失即异常 |
| **状态文件被删重来** | 删掉 `.publish-state.json` 从头开始 | 阈值：同一项目 24h 内 `start` 超过 2 次 → 告警 |

> ⚠️ **补一条更根本的（2026-09-12）：上面三件之外，还有一件状态机结构性防不住的——
> 代码本身是模型编的。**
> 状态机保证"按流程走完"，流程保证"包装合格"，包装合格建立在一个从没被检查过的前提上：**代码是真的**。
> 这个前提归 `publish-code-audit.md` 的 N0.5 / N4.5 管。
> **没有那一关，状态机走得越标准，越是把一个假东西包装得整齐。**

**结论：状态机把"走偏"从"大概率"降到"需要刻意绕过"。**
要 100% 防住，得靠 hook 在工具调用层拦截——那是另一个层级的工程，当前不做。

---

## 六之二、最大的洞：流程是开环的

前面所有闸门——脚本、裁判、状态机、三权分立——**都是代理人**。
它们保证的是"**按标准做对**"，不保证"**标准本身是对的**"。

**真正的裁判是市场**：下载量、star、issue。

### 现状：发布完就收口，从不回收

```
N0 → N1 → N2 → N3 → N4 → N5 → N6 → N7 收口 → 【结束】
                                              ↑
                                          这里就断了
```

现在的流程是**开环**的：标准从哪来？从 `publish-quality.md` 里来。
那份规范是谁写的？**我写的。依据是 3 个样本的观察。**

**我们全体（脚本 + 裁判 + 挑刺专家）都在对一个我自己定的标准负责。**
如果标准本身偏了，流程走得越标准，错得越整齐。

### 用现有数据打脸

| 发布物 | 我的评分 | 市场数据 | 一致吗 |
|---|---|---|---|
| `everytime-novel` | 未评 | **1066 下载** | — |
| `selfopt` | 用户视角 **1 分** | 201 下载（topics 为 0） | **不一致**：我判 1 分，市场给了 201 |
| `no-bb` | README **5 分** | 35 下载 | **严重不一致**：我判 5 分，市场只给 35 |

**这两个不一致，说明我的评分标准至少是残缺的。**

可能的解释（都是假设，未经证实）：
- selfopt 201 下载：可能是 SkillHub 站内流量红利，与质量无关；也可能是"Python 优化"这个需求本身硬
- no-bb 35 下载：**可能压根没人看见**（名下 GitHub 仓库全部 0 star）；也可能受众太窄

**重点是：我们不知道原因。而且——因为曝光本身不可控，光看数据也分不清。**

### 数据只能证伪，不能证实

既然曝光不可控，**所有下载量都混着不可控的曝光噪声**，没法从数据里分离出"质量好不好"。

| 现象 | 能推出什么 |
|---|---|
| 下载高 | ❌ 推不出"发布物好"（可能是流量红利、需求硬） |
| 下载低 | ❌ 推不出"发布物差"（可能压根没人看见） |
| **明显差却下载高** | ✅ **唯一有效用法**：说明标准可能漏了什么 → 人去查 |

**数据是提出疑问的，不是给出答案的。没有自动校准这回事。**

### 怎么补：闭环最后一段

```
N7 收口
  ↓
N8  发布后 D+7 / D+30 自动拉数据（gh stars + sh downloads）
  ↓
N9  与基线对比：涨了 / 持平 / 没起色
  ↓
N10 没起色 → 触发复盘：是发布物问题，还是标准问题？
  ↓
    标准问题 → 回头改 publish-quality.md（**这一步必须人来**）
```

**能力其实已经有了**：`daily_report.py`（下载量趋势）、`check_downloads.py`（快照 + 增量）。
**缺的只是把它接进发布流程，并定义"没起色"的阈值。**

### 一条硬规则

**不许用 AI 自己改标准。**
`publish-quality.md` 是标准的载体，如果允许流程自动修改它，就变成了"自己判自己合格"——
正是这一整套设计要防的事。

数据回收 → 人看数据 → 人改标准 → 标准进流程。

---

## 七、与现有机制的关系

| 已有 | 作用 | 本文件补的是什么 |
|---|---|---|
| `preflight_publish_check.sh` | 发布前凭据/痕迹闸门 | 只是 N6 一道闸门，管不了全流程 |
| `publish-quality.md` | 内容标准 | 标准有了，没有"必须走"的保障 |
| `publish-expert-team.md` | 谁做什么、约束什么 | 角色定了，没有进度与顺序的强制 |
| **本文件（状态机）** | — | **把上面三者串成不可跳过的执行链** |
