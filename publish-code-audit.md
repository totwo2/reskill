# publish-code-audit.md — 第三方源码审计闸门（N0.5 / N4.5）

> **本文件管的是整条链此前完全没人管的一件事：**
> 前面所有闸门（质量、裁判、交叉校验）审的都是**包装**——README、SKILL.md、元数据。
> **没有一道闸门审"代码本身是不是老高要的、是不是真能跑"。**
>
> 于是可以出现这种完美局面：README 过闸、SKILL.md 过闸、裁判给 4 分、双平台发齐——
> **而里面的代码是模型按自己想法编的。**

---

## 零、这一关要解决的三件事

| # | 问题 | 典型表现 |
|---|---|---|
| **1** | **需求有没有真实现** | 事实表写"支持自动扫描"，源码里 `auto_scan()` 是个空壳 / 根本没被调用 |
| **2** | **有没有超纲、超纲对不对** | 老高要个扫描器，代码里多出一套插件架构 + 日志系统 + 配置中心（**没人要，模型自己加的**） |
| **3** | **是不是模型自己编的** | 文档写 `pip install selfopt`，实际无 `setup.py`；调用不存在的方法；import 不存在的模块 |

**第 3 条是最毒的**：它长得跟"写错了"一模一样，但本质是**幻觉**——
模型不是在实现，是在**生成一个看起来像实现的东西**。

> 已发生的实例（2026-09-12，selfopt）：
> 写手写出 `pip install selfopt` 作为安装方式。实测：项目里**没有 setup.py、没有 argparse**，
> 真实入口是 `demo()` / `auto_scan()` / `adopt()` 三个函数。
> 那不是笔误，是模型根据"一个 Python 项目应该有安装方式"这个**先验**编出来的。

---

## 一、审计代理的定义

### 身份设定（拉起时必须原样写进 prompt）

> 你是**第三方代码审计**。这些代码不是你写的，你也不参与写、不参与修。
> 你的活只有一件事：**判断它是不是真的做到了需求方要的事，以及有没有做到它声称做到的事。**
> 你对需求方（老高）负责，不对写代码的人负责，也不对流程负责。
> 你的判定必须能被第三方复现——**给不出锚点的判定等于没判**。

### 工具白名单

| 允许 | 禁止 | 理由 |
|---|---|---|
| `Read` `Glob` `Grep` **`Bash`（限只读命令）** | `Write` `Edit` | 它必须**能验证**，不能只读。但**不许改任何东西** |

**Bash 白名单（只这些，别的拒绝）：**

```bash
python3 -c "import <mod>"          # 模块是否真的存在
python3 -c "<调用语句>"              # 函数是否真的能调
ls / stat / find <只读>              # 文件是否真的存在
grep -rn "<符号>" <dir>              # 追调用链
wc -l / head / sed -n 'N,Mp'        # 定位锚点
git log --oneline                    # 看真实提交历史
```

**为什么不能只给只读三件套**：
"这函数存不存在"这种问题，**读是读不出来的，得真调一下**。
只给 Read 的审计代理，最后只能写"代码看起来是实现的"——那就退化成包装审查，白设这一关。

### 三条禁区（拉起时明确告知）

1. **不看 README / SKILL.md 当事实来源**——那是要被审的对象（N4.5），不是依据
2. **不读需求蒸馏文件以外的聊天内容**——聊天记录里的中间方案会被推翻，读全量等于把已废掉的念头当成需求
3. **不自己判"这样更好"**——只判"是不是要的"和"能不能跑"。觉得该更好，写进"存疑"栏交给人

---

## 二、N0.2：聊天记录 → 需求清单（审计的判据从哪来）

**审计的前提是知道"老高到底要什么"。这个东西只存在于聊天里，代码里没有。**

### 但聊天记录不能直接喂给审计代理

| 直接喂的后果 | 原因 |
|---|---|
| 太长，塞不进上下文 | 一次发布前的讨论常有几万字 |
| **被推翻的中间方案会被当成需求** | 老高说过"要不要加 CLI"，后来否了——审计会拿这句去判"CLI 没实现 = 不合格" |
| AI 的附和会被当成共识 | 聊天里大量"对，我觉得也是"是模型的回声，不是需求 |

### 所以必须先蒸馏

**`.publish-staging/requirement-brief.md`** —— 由主 agent 从当前会话抽取，**每条需求带老高原话引用**：

```markdown
# 需求清单（审计判据唯一真源）

> 来源：2026-09-11 会话。以下每条必须附老高原话；**没有原话的条目不许写**。
> 被推翻的念头另列"已否决"区，审计不得引用。

## R1 一句话要什么
> 老高原话：「就 B」——AI 智能体自优化方向（非 Python 优化红海）

## R2 必须做到
| # | 需求 | 老高原话 | 出处 |
|---|---|---|---|
| 1 | 扫描本地 Python 项目，找出可优化的热点 | 「改完先不发布，等 C 出结果」 | 2026-09-11 会话 |
| 2 | 优化建议必须能自证有效（可采纳闸门） | 「这数据是有针对域的，还有些域是负的」 | 同上 |

## R3 明确不要（禁区）
| # | 不要什么 | 老高原话 |
|---|---|---|
| 1 | 不要吹收益数字 | 「中位 1.5x 是假的，有负域」 |
| 2 | 不要把不存在的安装方式写进文档 | 「pip install selfopt 实测没有」 |

## R4 已否决（审计禁止据此判定）
- 「给 selfopt 补个 CLI」——提出过，**未采纳**，不算需求

## R5 我（老高）对这条清单的确认
signed_by: 老高   confirmed_at: ____
```

### 蒸馏的三条硬规则

| 规则 | 内容 |
|---|---|
| **原话原则** | 每条需求必须附老高原话。**AI 归纳出来的、没有原话支撑的，不许进 R2** |
| **否决区必填** | R4 不许为空。空着说明没仔细翻聊天，审计会拿废案当需求 |
| **人工确认** | **R5 跟 N1 事实表一起签字**——不新增介入点，只增加签字内容 |

**为什么需求清单要跟事实表一起签**：
它们其实是同一件事的两面——事实表说"这东西对外是什么"，需求清单说"这东西本来要做什么"。
分两次签 = 两张纸可能对不上。**一次签，签字时必须对一眼。**

---

## 三、N0.5：审计代理第一跑 —— 源码 vs 需求

**输入**：`requirement-brief.md`（R1–R4）+ 项目源码
**输出**：`.publish-staging/audit-code.md`，三张表

### 表 A：覆盖表 —— 需求有没有真实现

| 需求 | 判定 | 代码锚点 | 验证方式 |
|---|---|---|---|
| R2-1 扫描本地项目找热点 | ✅ 已实现 | `selfopt/scanner.py:42` `def auto_scan()` | `python3 -c "from selfopt import auto_scan"` → exit 0 |
| R2-2 建议可自证 | ⚠️ 部分实现 | `selfopt/gate.py:88` adopt() 存在但未被主流程调用 | grep `adopt(` 全项目 → 仅 2 处，均非入口 |
| R2-3 … | ❌ **未实现** | **无锚点** | grep `<关键词>` → 0 命中 |

**判定只有三种，没有"应该实现了"：**
- ✅ **已实现**：有锚点 + 验证命令跑通
- ⚠️ **部分实现**：有锚点但追不到主入口 / 边缘分支没写
- ❌ **未实现**：**无锚点**。文档里写了、代码里没有 —— 这就是幻觉

### 表 B：超纲表 —— 多出来的东西，是必要还是加戏

| 代码 | 需求里有吗 | 追调用链 | 判定 |
|---|---|---|---|
| `selfopt/plugins/` 插件架构 | ❌ 无 | grep → **0 处被主入口调用** | 🔴 **加戏，建议删** |
| `selfopt/config.py` 配置中心 | ❌ 无 | grep → 仅被自己 import | 🔴 **加戏，建议删** |
| `selfopt/io_trace.py` | ❌ 无 | → `profile.py` → `auto_scan()` **主入口可达** | 🟢 **必要超纲，补登记进 R2** |
| `selfopt/xx.py` | ❌ 无 | 追不到也删不掉（有外部引用） | ⚪ **存疑，问人** |

**超纲判定只有一个硬判据：把调用链追到主入口。**

```
grep -rn "<这个模块的符号>" .
  → 命中只在自己文件内 = 死代码 = 加戏
  → 能一路追到主入口    = 必要（需求清单写漏了，补登记）
  → 追不到但删不掉      = 存疑，交人
```

**这条判据是硬的**（grep 可复现），不是"我觉得没必要"。
**"超纲的对不对"不用靠审美判断，靠"它是否在需求达成路径上"。**

> ⚠️ **必要超纲必须补登记进 R2，不能就这么过去。**
> 需求清单是审计判据的唯一真源——代码里有、需求里没有，下次审计还会判它超纲。
> 补登记 = 老高确认"这确实是我要的（或者达成它必须有的）"。

### 表 C：幻觉表 —— 是不是模型自己编的（**本关的核心**）

**这张表专抓"看起来像实现，实际是生成"的东西。**

| 检查项 | 怎么查（必须是实跑，不是读） | 命中 = 幻觉 |
|---|---|---|
| **入口不存在** | 文档/代码里写的安装或启动方式，实跑一遍 | `pip install x` 无 setup.py / `python -m x` 无 `__main__.py` |
| **方法不存在** | 逐个 `python3 -c "from mod import fn"` | ImportError |
| **模块不存在** | `python3 -c "import <mod>"` | ModuleNotFoundError |
| **路径不存在** | 代码里引用的文件路径 `ls` 一遍 | No such file |
| **空壳函数** | grep `pass$` / `TODO` / `NotImplementedError` | 函数体是占位的 |
| **参数对不上** | 定义签名 vs 调用处传参 | 少参/多参 |
| **声称的实测没做过** | 找测试文件 / 找命令输出记录 | 无测试、无日志 = **"实测"是编的** |

> **最后一条最要命**：AI 说"我实测过了"，代码里没有测试文件、没有命令输出留痕——
> **那"实测"两个字就是幻觉。** 本关一律按"未实测"处理。

### 输出格式（强制）

每张表的每一行，**锚点列和验证方式列不许为空**：

```
❌ 空锚点行 = 无效判定，脚本拒收（audit-code.md 校验：锚点列空 → exit 1）
```

**这跟 R-NO-CONFAB 是同一条规矩**：给不出锚点就是我不确定，不许流畅地编一个。

---

## 四、N4.5：审计代理第二跑 —— 文档 ↔ 实现一致性

**第一跑在写文档之前（源码 vs 需求）。第二跑在写文档之后（文档 vs 源码）。**

**为什么必须跑第二次**：
第一跑之后，写手会产出 README / SKILL.md。**写手是 LLM，它会编。**
selfopt 事故就出在这里：代码没问题（第一跑能过），**是写手写出了不存在的安装方式**。

**输入**：`.publish-staging/README.md` + `SKILL.md` + 源码
**输出**：`.publish-staging/audit-docs.md`

### 检查清单（每条都要实跑）

| 文档里写的 | 实跑 | 不符 = fail |
|---|---|---|
| 安装命令 | 原样执行（在临时目录，不污染项目） | 命令不存在 / 报错 |
| 快速开始的每个命令 | 逐个跑 | 跑不通 |
| 提到的每个入口（函数名/文件名） | `grep` 全项目 | 0 命中 |
| 每个数字（1.5x / 30%） | 回 fact-sheet.md 找出处 | 无出处 = 编的 |
| 每个"支持 XX" | 找对应实现锚点 | 无锚点 = 编的 |

**判据一句话：文档里出现的每一个可执行、可定位的东西，都必须能在源码里找到锚点。**
找不到 = 幻觉，打回 N2。

> 这条其实就是把 **R-NO-CONFAB（事实必须带锚点）** 从"AI 说话"扩展到"AI 写的文档"。
> 同一条病，两个发作位置。

---

## 五、不过怎么办

| 表 | 判定 | 去向 |
|---|---|---|
| A 覆盖 | 有 ❌ 未实现 | **打回改代码**（不是改文档）——或者改需求清单：这功能其实不要了？**交老高定** |
| A 覆盖 | 有 ⚠️ 部分实现 | 列清楚差在哪，交老高定：补齐 or 从需求清单删掉 |
| B 超纲 | 有 🔴 加戏 | **删掉**，或老高明确说"留着" → 补登记进 R2 |
| B 超纲 | 有 🟢 必要 | **补登记进 R2**，不是就这么过去 |
| B 超纲 | 有 ⚪ 存疑 | **必须问老高**，审计代理不许自己判 |
| C 幻觉 | 任一命中 | **一律打回**。幻觉不是"小瑕疵"，是这东西到底存不存在的问题 |
| N4.5 文档 | 任一 fail | 打回 N2 重写，**最多 3 轮** |

**B 表的 ⚪ 存疑是硬的人介入点**：审计代理追不动调用链又删不掉的代码，
它必须写"我判不了，原因是 X"，**不许蒙一个是必要还是加戏**。

---

## 六、为什么这一关不能省

| 省掉会怎样 | 具体后果 |
|---|---|
| **包装全过、代码是编的** | 用户装完发现没这功能 → 这是**骗人**，比 README 写得差严重得多 |
| **事实表签在谎言上** | N1 的"收益数字"来自实现。实现是假的，签字就是给谎言盖章，后面所有节点全错 |
| **超纲累积** | 模型每次都"顺手加一点"，三轮之后项目里一半是没人要的东西，没人知道哪些能删 |
| **不可回滚** | 发布物出去了，用户已经装了。**包装能改，骗人不能改** |

**一句话：**

> **N0–N4 保证"说得对"，N0.5 保证"做的就是这个"，N4.5 保证"说的就是做的"。**
> 少了后两个，整条链在审一张漂亮的包装纸里面包的是什么。

---

## 七、与其他节点的关系

```
N-1  方向闸门（老高答三问：不发也是一种结果）
  ↓
N0   形态判定（gh / sh 各发什么形态、是否双发）
  ↓
N0.2 需求蒸馏（聊天 → requirement-brief.md，带老高原话）
  ↓                                    ← 【本文件】
N0.5 第三方审计·第一跑（源码 vs 需求：覆盖 / 超纲 / 幻觉）
  ↓  不过 → 改代码 or 改需求（老高定）
N1   事实表 + 需求清单一起签字（老高）
  ↓
N2-G / N2-S  写包装
  ↓
N3-G / N3-S / N3-X  形式闸门 + 交叉校验
  ↓
N4.5 第三方审计·第二跑（文档 vs 源码：每个命令/入口/数字都要锚点）
  ↓  不过 → 打回 N2，≤3 轮
N5 装配 → N6 发布 → N7 收口
```

| 节点 | 谁跑 | 管什么 |
|---|---|---|
| N0.2 | 主 agent 蒸馏 → **老高签字** | 需求唯一真源（带原话 + 已否决区） |
| **N0.5** | **只读子 agent（第三方身份）** | **源码是不是要的、是不是真能跑** |
| **N4.5** | **同一个审计代理（独立第二次拉起）** | **文档说的能不能在源码里找到** |
| N3 系列 | 脚本 | 形式（有没有、格式对不对） |
| N4 系列 | 裁判 LLM | 表达（用户/大模型看不看、懂不懂） |

**职责不重叠：脚本管形式、裁判管表达、审计管真实。**
前两个都建立在"代码是真的"这个前提上——**审计是唯一在检查那个前提本身的。**

---

## 八、待实现

- [ ] 审计代理的 prompt 模板（`.publish-staging/prompts/auditor.md`），身份段原样照抄 §一
- [ ] `scripts/verify_audit_report.py` —— 校验证据完整性（锚点列空 / 无验证命令 → exit 1）
- [ ] `scripts/trace_callchain.py` —— 表 B 的调用链自动追（从符号 grep 到主入口，输出可达/不可达）
- [ ] `scripts/check_doc_anchors.py` —— N4.5 自动化：抽 README 里的 ```bash 代码块 + 反引号标识符 → 逐条实跑 / grep
- [ ] `.publish-staging/requirement-brief.md` 模板 + 与 fact-sheet.md 的联动校验（两条纸对不上 → 拒绝签字）
