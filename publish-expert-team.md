# publish-expert-team.md — 发布专家团队的编排与约束

> 专家团队是 LLM，换皮的 LLM。**它解决的是"立场"问题，不是"控制"问题。**
> 控制只能靠能力边界和代码闸门。本文件定义怎么把专家关进笼子里干活。

---

## 零、一条铁律：闸门不是专家的工具

| | 工具 | 闸门 |
|---|---|---|
| 语义 | 你**可以**调用 | 你**必须**通过 |
| 谁能跑 | 持有者自己决定 | 编排层强制执行 |
| 结果 | 可以参考、可以忽略 | exit code，没有商量 |

**把闸门交给专家 = 让它既当运动员又当裁判**：它可以选择不跑、可以把失败解释成"其实没问题"、可以调低阈值让它过。

**正确位置：闸门不能在任何被审者手里**（包括执行流程的主 agent）。

> ⚠️ **本节有一处需要修正的漏洞：**
> 前文写"闸门在编排层（主 agent）手里"——**但主 agent 就是那个会偏的 LLM**，
> 等于把闸门交给被审者自己判定自己过关。
> 修正后的分工：
> - **脚本闸门** → 编排层跑，管形式（有没有、格式对不对）
> - **语义闸门** → **独立的裁判 LLM** 跑，管语义（做得对不对、改得对不对）
>   详见 **`publish-llm-gate.md`**：流程裁判四问、双裁判架构、裁判不失效的条件。
>
> 一句话：**脚本管形式，裁判管语义，两者都在被审者之外。**
>
> ⚠️ **2026-09-12 补：这两者都不管"代码是不是真的"。**
> 脚本查"有没有这个文件"，裁判查"这话用户看不看得懂"——
> **都建立在"代码是真的"这个从没被检查过的前提上。**
> 补第三极：**审计管真实**（`publish-code-audit.md`）。
> **脚本管形式、裁判管表达、审计管真实，三者都在被审者之外。**

类比：CI 里的测试不是"给开发者的工具"，是 push 之后自动跑的关卡，不过就 merge 不了。

---

## 一、约束专家的四层（缺一层都不牢）

| 层 | 机制 | 拦住什么 | 强度 |
|---|---|---|---|
| **1 能力** | 拉起时限定工具白名单 | 让它**做不了**（挑刺专家不给 Write/Edit/Bash，物理上无法改文件） | 硬 |
| **2 隔离** | 产物写暂存区，不进项目 | 写了也**不生效**，须经编排层 apply | 硬 |
| **3 闸门** | 编排层跑脚本，exit code 说话 | 不合格**发不出去** | 硬 |
| **4 审计** | 全项目在 git 下，发布前看 `git diff` | 万一漏网，可查可**回滚** | 兜底 |

**层 1 是唯一真正的约束**——不是"告诉它别做"，是"让它做不了"。
提示词里写"你不许改文件"是软的；不给它 `Edit` 工具是硬的。

---

## 二、角色配置与工具白名单

> ⚠️ **角色与工具白名单的单一真源已移到 `publish-team-roster.md`。**
> 本节原先定义 4 个角色，与 `publish-llm-gate.md`（裁判 2 个）、`publish-code-audit.md`（审计 1 个）
> 三处重复定义、互相打架（2026-09-23 规整记录见名册 §五）。
> **改角色、改权限，只改名册那一份，本文件不再重复。**

**名册速览（详见 `publish-team-roster.md` §一）：**

| 编号 | 角色 | 一句话职责 | 归属文件 |
|---|---|---|---|
| **J1** | 流程裁判 | 执行对不对（四问） | `publish-llm-gate.md` |
| **J2 / J3** | 质量裁判 ×2 身份 | 产物好不好（会不会下载 / 能不能照着干） | `publish-quality.md` §五 |
| **J4** | 第三方代码审计 | 代码是不是真的（唯一检查这个前提的） | `publish-code-audit.md` |
| **E1** | 用户视角写手 | 写两线包装（README / SKILL.md / description / tags / topics） | `publish-quality.md` |
| **E2** | 发布执行者 | 跑发布命令 | `publish.md` |

**主 agent 的权限以 `publish-llm-gate.md` §六之二为准：只剩 `Bash`（限 `publish_flow.py`）+ `Agent`，
不给 `Read` / `Write` / `Edit`。**（本节此前写的"唯一有完整权限"已作废。）

### 落地要点

- **挑刺用户直接用只读型子 agent**（如 `Explore`）——天然无写权限，省去声明。
- **写手"禁读源码实现"是硬要求**：selfopt 的病就是写手读了 sign test 之后，觉得那是最好的开场白。
  给它看的是"这东西对外解决什么问题"，不是"它怎么实现的"。
- **执行者只能跑固定几条命令**：`gh release create` / `skillhub publish` / 实例数据移出移回脚本 / 闸门脚本。
  自由 Bash = 自由改内容。

---

## 三、完整流程（7 步，编排层全程在主 agent）

> ⚠️ **双平台不是"第 6 步发两次"，是从第 2 步起就分叉两条线。**
> 完整的双平台结构见 **§四之三**。本节是单线抽象版，用于理解主干。

```
0. 主 agent：确认代码已完成、发布目标（平台 + 版本号）
      ↓
1. 拉起【挑刺用户】（只读型）
   输入：当前 README + description + topics
   输出："我不会下载，因为……" / "会下载，因为……"
      ↓
2. 拉起【用户视角写手】
   输入：项目对外功能说明 + 挑刺结果 + publish-quality.md 规则
   输出：写到 .publish-staging/ 的草稿（README / description / topics）
      ↓
3. 编排层跑质量闸门：python3 scripts/preflight_quality_check.py --dir .publish-staging/ --platform github
   exit 0 → 继续
   exit 1 → 把失败项原文回传给写手重来（最多 3 轮，超限则自动不发 + 留报告）
      ↓
4. 编排层 apply：暂存区内容写入项目（此时才生效）
      ↓
5. 编排层跑发布闸门：bash scripts/preflight_publish_check.sh .
   exit 0 → 继续；exit 1 → 中止
      ↓
6. 编排层执行发布命令（gh + skillhub 双平台，同版本号）
      ↓
7. 输出契约三行：版本号 / 平台链接 / 同步状态 → 收口，不输出改进建议
```

**步骤 3、5、6 只能由编排层跑。专家在任何一步都插不上手。**

---

## 四、为什么不用平级 Team 型

Team 型（peer-to-peer）里每个成员权限对等，谁都能写文件——**适合讨论，不适合执行约束**。

发布是**执行链**，不是圆桌会议。用主 agent 编排 + 受限子 agent：
- 主 agent = 编排层（唯一有完整权限、也是唯一负责任的）
- 子 agent = 被咨询的专家（能力受限、产物隔离）

---

## 四之二、蜂巢模式的适用边界（2026-09-11 定论）

蜂巢（专家并行产出、不经主理人转译、做完即交付）成立的前提是**切片清晰且互不耦合**。
按这个标准，发布任务要分两种场景看：

| 场景 | 用蜂巢？ | 理由 |
|---|---|---|
| **单项目日常发布** | **不用** | 只有 3~4 片（README / 元数据 / SKILL.md / 发布执行），且 README 与 description、topics 是"同一件事的三种长度"——**强耦合**，切开反而容易写裂。片太少，并行省不了时间，白增协调成本 |
| **批量修补多个发布物**（如一次性修 10 个仓库 + 11 个 skill） | **用** | 每个仓库是一片，片之间**真独立**（改 selfopt 的 README 不影响 reskill 的）。这时并行有实效，且互不干扰 |

**结论：蜂巢是给"多片真独立"的批量任务用的，不是给单次发布用的。**

单项目发布沿用本文第三节的 7 步流程；批量修补时，把每个仓库当作独立片交给专家并行处理，每片各自过闸门，不合格只打回那一件。

---

## 四之三、双平台 = 两条线（2026-09-11 定论）

GitHub 什么形态都能发，SkillHub 只收 skill——但"能发"的范围比 skill 本身大一圈：
**本身就是 skill 就直接发；不是 skill 但装得进 skill 包，就做成 installer 形态；装不进就不发。**

### 关键判断：分叉点在第 2 步，不在第 0 步

第 0 步只是**判定**（这个项目在两个平台各以什么形态出现、要不要双发），判定完仍然是一条线。
真正分叉在**第 2 步写发布物**——因为两线的受众、语言、入口文件、校验规则全不同。

分叉点之前必须共享，分叉点之后必须分开。**共享的是事实，分开的是包装。**

### 结构图（文字版）

```
【共享段】
  N-1  方向三问        老高                搜哪个词 / 被什么扎着 / 跟你发过的区别（答不出不发）
  N0   形态判定        脚本（主 agent 只调） 决定：gh 发什么 / sh 发什么 / 是否双发
  N0.2 需求蒸馏        执行子 agent + 老高  聊天记录 → requirement-brief.md（带原话 + 已否决区）
  N0.5 第三方审计①     只读子 agent         源码 vs 需求：覆盖 / 超纲 / 幻觉（逐条带锚点）
       ↑ 不过 → 改代码 或 改需求（老高定）。这一关不过不许往下走
  N1   事实表 + 需求清单一起签字
                      执行子 agent 起草 → 老高签字
                      一句话定位 · 收益数字 · 目标用户 · 禁用词 · framing（evidence/block/bound）
                      ↓ 这是两线唯一的内容来源，不许各自发挥
        ┌─────────────┴─────────────┐
【G 线】                          【S 线】
  N2-G 写 GitHub 包装               N2-S 写 SkillHub 包装
    README.md（中文主文档）           SKILL.md（中文，frontmatter 严格）
    README_EN.md（英文镜像）          description + 触发词
    repo description                 displayName（中文）
    topics（英文，≥10，两组词）        tags（**中文，单行方括号**）
       ↓                                ↓
  N3-G 闸门 G                        N3-S 闸门 S
       └─────────────┬─────────────┘
                     ↓
  N3-X  交叉校验（防两线漂移，脚本）
        同一句话定位？同一组数字？slug↔仓库名可对应？版本号一致？
                     ↓
【并行挑刺】
  N4-G 国际读者评委评 README          N4-S 中文读者评委评 SKILL.md
                     ↓
  N4.5 第三方审计②（**同身份、新实例** —— 不是"同一代理续跑"，见名册 §7.7）
       文档 vs 源码：README/SKILL.md 里每个命令·入口·数字，都要在源码找得到锚点
       ↑ 防的是"代码没问题、写手编了个不存在的安装方式"（selfopt 事故原型）
                     ↓
【汇合段】
  N5   装配（暂存区 → 项目，git diff 可审 —— 给人看）
  N6-S 打包（出干净副本，脚本 `pack_skillhub.sh`，不看 .gitignore）
  N6   推文件（凭据/痕迹闸门 exit 0 → 推上远端）
  N6   建 release（按布局选工具；**在仓库根跑**，见名册 §7.13）
  N7   收口三行：版本 / 链接 / 同步状态
```

> ⚠️ **本图是速览，不是真源。**
> **流程真源** = `publish-flow-control.md` §三之二 节点矩阵（准入 / 准出一票否决 / 打回路由）；
> **角色真源** = `publish-team-roster.md` §三（谁在场 / 向 reskill 取什么）。
> 图里不重抄这两处的细节 —— **重复必漂**：2026-09-23 本图就漂了 5 处
> （N0/N0.2/N1 还写"主 agent"、G 线还写"英文为主"、S 线把平台字段写成 `topics`）。

### 各节点产出什么、硬约束是什么

> **谁做 → 见 `publish-team-roster.md` §三**（角色单一真源）。
> **准入 / 准出一票否决 / 打回路由 → 见 `publish-flow-control.md` §三之二 节点矩阵**（流程单一真源）。
> **本表只写那两处没有的东西：每个节点产出什么、硬约束是什么。**
>
> ⚠️ 本表原先还有一列「谁做」，2026-09-23 **删掉** —— 它与名册重复，且已经漂了 5 处：
> N0.2/N1 还写"主 agent 起草"（主 agent 没有 `Write`）、N5 还写"主 agent `git diff`"
> （主 agent 没有 `Bash(git)`）、N6 发布没拆三段、N4.5 还写"同一审计代理"（早已定为"同身份、新实例"）。
> **重复的清单一定会漂，删掉比同步便宜。**

| 节点 | 产出什么 | 硬约束 |
|---|---|---|
| **N0 形态判定** | 形态三分支（A 原样发 / B installer 形态 / C 不发）+ 是否双发 | 脚本查入口文件 + 体积判据；**"双发"不是默认值，要逐次判定** |
| **N0.2 需求蒸馏** | `requirement-brief.md`（每条带老高原话 + **已否决区**） | 无原话的条目不许写；已否决区不许为空；**跟事实表一起签字，不新增介入点** |
| **N0.5 源码审计①** | 三张表：覆盖（需求↔锚点）/ 超纲（追调用链到主入口）/ 幻觉（实跑验证存在性） | 禁 Write/Edit；只读 Bash 白名单；**锚点列空 = 无效判定**；幻觉任一命中一律打回 |
| **N1 事实表** | 一句话定位 + **收益数字** + 目标用户 + 禁用词 + **`framing` 字段**（每个数字标 evidence/block/bound） | 数字不许 AI 编；无实测就写"未实测"，禁写"显著提升"；**两张纸（事实表 + 需求清单）必须对得上**；缺 `framing` → 打回 |
| **N2-G** | README（中文主文档 + `README_EN.md` 英文镜像）+ repo description + topics | 禁读源码；产物进暂存区；数字只能引用事实表 |
| **N2-S** | SKILL.md（中文）+ description + 触发词 + displayName + `tags`（中文，**单行方括号**） | 同上 |
| **N3-G 闸门** | exit code | topics ≥10 且两组词齐全 / README 前 30 行有数字 / 无中英逐段对照；不过打回 N2-G，≤3 轮 |
| **N3-S 闸门** | exit code | frontmatter 齐全 / 有触发词 / 用法在前 1/3 / 无开发日志与数学推导；不过打回 N2-S，≤3 轮 |
| **N3-X 交叉** | exit code | 两线定位句同源、核心数字一致、slug↔仓库名可对应、版本号数字一致。**防"同一个东西两个说法"的唯一关卡** |
| **N4-G / N4-S** | 打分（0–5）+ 理由 | 两个身份各评一次、可并行；无写权限只评不改；<3 分打回对应线 |
| **N4.5 文档审计②** | 锚点表 | 每条命令 / 每个入口 / 每个数字 → 实跑 or grep 找锚点；找不到 = 幻觉 → 打回 N2，≤3 轮 |
| **N5 装配** | 暂存区 → 项目 | `git diff` 可审计（**给人看** —— 主 agent 没有 `Bash(git)`，见名册 §一之四） |
| **N6-S 打包** | 干净副本 | **不看 `.gitignore`**（skillhub 打包不看）；实例数据须先移出；专家不碰打包脚本 |
| **N6 推文件** | 远端提交 | 推上去的内容与本地副本逐字一致 |
| **N6 建 release** | tag + release | 按布局选工具（`skills/<name>/` → `gh skill publish --tag`；根级 → `gh_release.py`）；**在仓库根跑** |
| **N7 收口** | 三行：版本 / 链接 / 同步状态 | 禁输出无 source 的改进建议 |

### 三条设计理由

**① 两线必须共用一个写手，不能并行给两个人。**
README 和 SKILL.md 形态不同，但"一句话定位 + 核心数字"是同一件事的两种长度。
交给两个专家并行写 = 写出两个版本，同一个东西两种说法。
**强耦合的东西不能切片**——这也是蜂巢在这里不适用的第二个理由（第一个是片太少）。

能并行的是 **N4 挑刺**：两个身份各评各的，互不干扰，且互不失真。

**② N3-X 交叉校验是两线结构特有的、必须新增的关卡。**
单线流程里不需要它。分叉之后没有它，两线必然漂移。
已发生的实例：`zhi-py-opt`（gh）vs `zhi-neng-py-jiao-ben-you-hua`（sh）——同一个东西两个名字，用户搜到也对不上号。

**③ N0 的"是否双发"要逐次判定，不能默认双发。**
SkillHub 只收 skill，但可发范围是三分支（详见 `publish-quality.md` §一）：

| 判定 | 结果 |
|---|---|
| **A** 本身就是 skill | sh 直发、gh 发项目仓库 → 双发 |
| **B** 不是 skill 但代码小、能整体塞进 skill 包 | sh 走 **installer 形态**（SKILL.md 作安装入口，代码随包分发，装 skill = 装软件）；gh 发项目仓库 → 双发 |
| **C** 不是 skill 且装不进 skill 包 | **sh 不发**，只发 gh |

**C 分支硬发 SkillHub 只有两种结果**：包一层空壳 SKILL.md（用户装了没东西），或形态不对被拒。
**这两种都是"为了发布而发布"，正是要根治的病。**

所以 N0 的输出允许是：只发 gh / 只发 sh / 双发（B 分支需说明 skill 包里装了什么）。

---

## 四之四、团队的本质 = 子代理 + 约束结构（2026-09-11 定论）

### 概念纠正：子代理之和 ≠ 团队

拉起 N 个子代理，只是"N 个 LLM 在跑"，不是团队。
**团队之所以成立，靠的是四样东西，子代理只是载体：**

| 要素 | 团队里是什么 | 本方案里对应什么 |
|---|---|---|
| **角色分工** | 谁干什么 | 执行子 agent / 裁判子 agent / 主 agent（纯调度） |
| **权限边界** | 谁能改什么 | 工具白名单（主 agent 不给 Write/Edit，裁判只读） |
| **交接协议** | 产物怎么传递 | **走文件系统，不走主 agent 的嘴**（`.publish-staging/` + `verdict-*.md`） |
| **仲裁机制** | 不合格谁打回、冲突谁说了算 | 脚本（形式）+ 裁判 LLM（语义）+ 老高（3 轮超限 / N1 签字） |

**归集是把能力放一起，升华是加了组织结构。升华的那一下就是约束结构。**

### 逻辑上是团队，实现上不用平级 Team 型

这是两个层面，别混：

| 层面 | 结论 |
|---|---|
| **逻辑概念** | ✅ 它就是团队——有角色、有分工、有仲裁、有交接协议 |
| **实现形态** | ❌ **不用平级 Team 型**，用「主 agent 编排 + 受限子 agent + 代码仲裁」 |

**为什么不能平级**：Team 型里成员权限对等，谁都能写文件、谁都能自评。
适合讨论，**不适合执行约束**——发布是执行链，不是圆桌会议。

平级团队在这里会出的具体事：每个专家都能改项目文件，且都能给自己的产出判 pass。

### 三种"回闸门"的去向，必须分清

| 情况 | 回到谁 | 绝不能回到谁 |
|---|---|---|
| **判定不合格**（语义层） | **审核专家（裁判 LLM）** | ❌ 主 agent——它已被削成纯调度，回它 = 还它裁量权 = 前面全废 |
| **形式不合规 / 推进** | 脚本 `publish_flow.py` | ❌ 任何 LLM |
| **需要人决策**（N1 签字 / N0.5 存疑项） | **老高** | ❌ 主 agent 替你决定。打回超 3 轮已改为**自动不发**，不再扣人 |

**主 agent 什么都不回。** 它只是按顺序发起调用的那只手。

### 一句话总结

> **团队是骨架，代码是筋，裁判是神经。**
> 只有骨架 = 换皮 LLM 乱来；只有筋 = 管形式不管语义；只有神经 = 无流程可依。

---

## 五、待实现清单

- [x] `scripts/publish_flow.py` —— **流程状态机**（2026-09-12 已实现，28820B）
  - `start --project --platforms --version`：三参数缺一即报错退出，不许猜、**不许默认双发**
  - `next`：打印当前节点 / 需产出文件 / 准入条件 / 约束
  - `done <节点>`：校验证据文件存在且合规 → 写 `.publish-state.json` → 推进
  - `status`：只读打印进度
  - **不提供 `skip` / `force` / `set-status`**；LLM 不直接写状态文件
  - 推进前校验前置节点全 `pass`，缺哪个打印哪个
- [ ] **裁判 LLM 配置**（详见 `publish-llm-gate.md`）
  - 流程裁判：只读子 agent，四问必书面回答，判语义不判形式，fail 必须附可验证理由
  - 质量裁判：两个身份（国际开发者 / 中文用户），给好样本与坏样本锚点
  - **裁判与执行者尽量不同模型**，至少不同角色设定与温度
  - 裁判只看产物，**不看执行者的自我陈述**
  - `.publish-staging/history/` 保留上一版，供"第 3 问（改的是不是被打回的点）"对比
- [x] `scripts/detect_publish_form.sh` —— **N0 形态判定**（2026-09-12 已实现，5695B；三分支自动判，输出 A/B/C + 是否双发）
  - 有无合规 `SKILL.md`（含 slug/displayName/description）→ A
  - 无 SKILL.md 但源码打包后 ≤ 阈值 且 无重依赖 且 单入口 → B（installer 形态）
  - 其余 → C（sh 不发，只发 gh）
  - 阈值默认 1 MB，可配；**判不定时必须停下来问人，不许猜**
- [x] `scripts/preflight_quality_check.py` —— 质量闸门脚本（**2026-09-12 已实现**，含 `--test` 自检）
      按平台分两个 profile：`--platform github` / `--platform skillhub`，规则不同
  - **G 线**：topics ≥10 且两组词齐全 / README 前 30 行有 `\d+%`·`\d+x` / 无连续中英逐段对照
  - **S 线**：frontmatter 全（slug/displayName/description）/ 有"触发词" / 用法章节在前 1/3 / 无开发日志与数学推导
  - **两线共通**：description 命中实现黑话词（hook / 注入 / 闸门 / 架构 / 证人集）→ exit 1
- [ ] `scripts/crosscheck_dual_platform.sh` —— **N3-X 交叉校验**（两线结构新增）
  - 两线"一句话定位"同源（关键词重合度）
  - 两线核心数字串一致
  - slug ↔ 仓库名可对应（禁止 zhi-py-opt vs 拼音全名那种）
  - 版本号数字一致（`v1.2.0` ↔ `1.2.0`）
- [ ] **第三方代码审计代理配置**（详见 `publish-code-audit.md`，**当前链上最大的洞**）
  - N0.2 需求蒸馏：聊天记录 → `requirement-brief.md`（每条带老高原话 + 已否决区），与事实表一起签字
  - N0.5 第一跑：源码 vs 需求 → 覆盖表 / 超纲表（追调用链到主入口）/ 幻觉表（**实跑验证**）
  - N4.5 第二跑：文档 vs 源码 → 每条命令·入口·数字都要锚点（防"写手编出不存在的安装方式"）
  - 工具：`Read` `Glob` `Grep` `Bash`（只读白名单），**禁 Write/Edit**
  - 锚点列空 = 无效判定（脚本拒收）；幻觉任一命中一律打回；⚪ 存疑必须问老高
- [ ] `scripts/verify_audit_report.py` —— 审计表证据完整性校验（锚点/验证命令缺失 → exit 1）
- [ ] `scripts/trace_callchain.py` —— 超纲判定自动化（符号 → 主入口，输出可达/不可达）
- [ ] `scripts/check_doc_anchors.py` —— N4.5 自动化（抽 bash 块 + 反引号标识符 → 实跑/grep）
- [x] `scripts/pack_skillhub.sh` —— N6-S 打包（自检 11/11）
  - 为什么必须有：skillhub 打包**不看 `.gitignore`**，排除表写死只有 `.git`/`.idea`/`.vscode`/`node_modules`/`__pycache__`
    ＋ `*.pyc`/`.DS_Store`/`Thumbs.db`（`skills_store_cli.py:2227`）→ `.publish-staging/`、脚本自测临时目录会被当正式文件传上去
  - 产出干净副本 → `skillhub publish "<副本路径>"`。**发布的是副本，不是源目录**
  - 同时剔除平台拒收的 `LICENSE`（400「不允许的文件类型」）与 `.gitignore`；许可靠 frontmatter `license:` 声明
- [x] ~~四个专家的配置文件（挑刺用户 ×2 身份 / 用户视角写手 / **第三方代码审计**）~~ —— **2026-09-23 定：不建**（见名册 §7.20.1）
  - 理由：与 §7.2「团队不独立成实体」冲突；配置文件会成为**第二处角色定义**，
    而"改一处忘一处"正是本轮反复在修的病灶。角色定义只留 `publish-team-roster.md` 一处，
    子 agent 启动时读名册。**从待实现清单撤掉，不是延后。**
- [x] `.publish-staging/` 暂存区约定 + `.gitignore` 排除
  - **2026-09-23 补**：`.gitignore` 只挡 git，**挡不住 skillhub 打包**；包内的暂存区由 `pack_skillhub.sh` 剔除
- [x] 与 `publish-quality.md` 的双维度打分对接（脚本管硬指标，人/挑刺专家管软指标）—— **2026-09-23 定：接线，不新建机制**（见名册 §7.20.4）
  - 硬指标已有现成件：`quality_metrics.py`（客观代理指标）+ `render_check.py`（同族病灶机器拦截）
  - 缺的只是"把这两个的输出接到打分表上"——那是接线，不是设计
