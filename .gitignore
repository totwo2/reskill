# Local config (contains real tokens)
settings/reskill_config.yaml
settings/notify_config.yaml
settings/cron_config.yaml

# Runtime artifacts (local monitoring data, do not publish)
settings/download_history.yaml
settings/feedback_report.md
settings/my_skills_snapshot.yaml
scripts/feedback_report.md

# SkillHub metadata
_meta.json

# Python cache
scripts/__pycache__/
__pycache__/
*.pyc

# Runtime state (local, do not publish)
settings/daily_state.json

# OS
.DS_Store

# Platform automation state (local env, do not publish)
.workbuddy/

# Author runtime publish logs (instance data, not template content)
settings/release_history/

# Publish pipeline staging (artifacts, verdicts, ledger — instance data)
.publish-staging/

# Internal decision logs (author notes; contain local paths & internal context —
# not for a public repo. Kept on disk locally.)
DECISIONS-*.md

# Archived third-party reference copy (no doc references it; contains platform
# paths. Kept on disk locally, not published.)
references/quibbler/
