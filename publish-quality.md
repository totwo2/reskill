# publish-quality.md — 发布物质量规范

> 代码功能没问题，问题全在"包在外面那一层"。
> 发布前必读。形态 → 给人看 → 给模型看 → 元数据，四道各有一票否决。

---

## 零、这份规范要对标什么

**基准线是老高亲手写的东西，不是 AI 生成的。**

一个必须诚实承认的事实：
已发布的项目里，**凡 README 能看的，都是老高自己一句句写的**；
凡 AI 按流程产出的发布物（简介、topics、SKILL.md、元数据），基本都不达标。
代码和想法都是老高的，AI 只负责实现——实现出来的"包在外面那一层"一塌糊涂。

所以本规范只有一个目标：
**让 AI 生成的发布物，达到老高手工写 README 时的那个标准。**

### 基准（老高亲写，作为标准参照，不作为 AI 能力证明）

- **no-bb 的 README**：痛点钩子 → 数据表格 → 真实素材引用（贴出模型"哦，会不会是……不，不是……"原文）→ 30 秒上手 → 装完验证。
  ⚠️ **no-bb 只有 README 达标**，同项目其他发布物同样不合格。
  它的作用是**让人看见标准长什么样**，不是证明"流程能产出这个"——恰恰相反，它证明流程产出不了。

### 现状（AI 生成，反面）

- **selfopt**：README 中英逐段对照、数学闸门开篇（sign test / p<0.05 / 交叉点 n*）；
  SKILL.md 前 110 行全是原理与开发日志，用法被埋到 50% 之后；
  frontmatter **没有 description**；topics 为 **0**（而它下载量 201，纯吃站内流量）。

**差距根源不是能力，是发布时没有被明确要求。** 本文件就是那个"明确要求"。

---

## 一、先定形态：两个平台是两种东西

**这是第一步，形态错了后面全白做。**

| | GitHub | SkillHub |
|---|---|---|
| 发布形态 | **普通项目仓库**（什么都能发） | **必须是 skill**；不是 skill 的东西只有"装得进 skill 包"才发，否则不发 |
| 入口文件 | `README.md`（项目主页） | `SKILL.md`（frontmatter 严格校验） |
| 打包方式 | 不打包，`.gitignore` 有效 | 打包**不看** `.gitignore`，需手动移出实例数据 |
| 主要受众 | 国际开发者（英文优先） | 中文用户（中文优先） |
| 版本写法 | 带 `v`：`v1.2.0` | 不带 `v`：三段 SemVer `1.2.0` |
| 必需字段 | `description` + `topics` | `slug` / `displayName` / `description` + 触发词 |
| 名称要求 | 项目名，稳定不轻易改 | 让中文用户一眼看懂干什么 |

### SkillHub 侧：不是所有东西都发，判定三分支

**SkillHub 只收 skill，但"能发"的范围比 skill 本身大一点。**

| 情况 | 判定 | 做法 |
|---|---|---|
| **A. 本身就是 skill** | ✅ 直发 | 有合规 `SKILL.md`，原形态发布，什么都不用包 |
| **B. 不是 skill，但代码体积小、能整体塞进 skill 包** | ✅ 走 **installer 安装器形态** | `SKILL.md` 作为安装与使用入口，原始代码放 `scripts/` 或 `assets/` 里随包分发，**装 skill = 装软件** |
| **C. 不是 skill，且装不进 skill 包** | ❌ **不发 SkillHub**，只发 GitHub | 体积大 / 需要编译 / 需要 pip 装依赖 / 是常驻服务 |

**B 的判据（三条全满足才算"装得进"）：**

1. **体积小**：源码（不含实例数据、不含 `.git`）打包后 ≤ 1 MB —— 阈值待老高拍板
2. **自包含**：无需编译、无需 `pip install` 第三方重依赖，单入口一条命令能跑
3. **可原地运行**：把文件放到目标位置就能用，不依赖外部环境配置

**B 的典型翻车**：把体积大的东西硬塞进 skill 包 → 装上慢、包膨胀、SkillHub 侧拒收或体验崩。
**C 的典型翻车**：为了"双平台都发"硬包一层空壳 SKILL.md → 用户装了没东西，这就是"为了发布而发布"。

> **判断原则：SkillHub 是"装机渠道"，不是"代码托管备份"。**
> 装上去不能立刻用的东西，一个都别发。

### 两种典型翻车（都实际发生过）

1. **把 GitHub 普通项目直接推 SkillHub** → 无 `SKILL.md` 或 frontmatter 缺 `slug` → 发布被拒 / 装不上。
2. **把 SkillHub 的 skill 原样推 GitHub** → README 是"技能使用说明"而非"项目主页" → 主页没有钩子、没有数据，路过的人不会停。

### 同一项目双平台必须同标识

反面教材：`zhi-py-opt`（GitHub）vs `zhi-neng-py-jiao-ben-you-hua`（SkillHub）。
同一个东西两个名字，其中一个还是拼音——用户搜到也对不上号，等于自断流量。

**规则：双平台 slug / 仓库名必须一致（或一眼可对应），`displayName` 用中文且不含缩写黑话。**

---

## 二、给「人」看的东西：仓库主页 README

### 黄金四段（顺序不能变，前 3 秒决定他走不走）

| 段 | 写什么 | 判据 |
|---|---|---|
| **1 钩子** | 用户正被什么扎着（一句场景，不是功能介绍） | 不写自己，写用户的疼 |
| **2 收益** | 按项目类型二选一（见下节），最好成表格 | 优化类=数字可复算 · 功能类=替你做的事可指认 |
| **3 上手** | 30 秒内能跑起来的最短路径 | 复制粘贴即可执行 |
| **4 验证** | 装完怎么确认真生效 | 一条命令看到结果 |

**然后**才是原理、架构、边界。原理放前面 = 用自嗨赶客。

### 第 2 段分两种，别混（2026-09-15 修正）

**问法：这个收益，是跟一个可对照的基线比出来的吗？**

| | **优化类 `kind: perf`** | **功能类 `kind: feature`** |
|---|---|---|
| 产物是什么 | 对**已有东西**的改进 | **以前没有**的能力 |
| 有没有基线 | 有（改前 vs 改后、A 方案 vs B 方案） | 没有（原本根本没这能力） |
| 第 2 段写什么 | **问数字**：省多少 / 快多少 / 少几步 | **问动作**：它自动替你做了什么 |
| 判据 | 数字必须可复算，且说清跟什么基线比 | 必须能具体指认「你本来得亲手做的哪几件事」 |
| 例 | selfopt「1.5x faster」· no-bb「思考量 -33%~-51%」 | reskill「8 个环节自己走完，你只说一句话」 |

**功能类不要去凑数字。** 没有基线，就没有"提升了多少"可言。
硬编一个「效率提升 40%」正是本文件要防的「不可复算的数字」——
**为过闸门而造假，比不过闸门更坏。**

**「少几步」要写成对比形态**：`8 个环节 → 1 条命令`。
写成陈述句（「把多件事合成一条」）闸门认不出，读者也读不出差了多少。

**怎么声明**：`SKILL.md` frontmatter 写 `kind: perf` 或 `kind: feature`。
不写也能跑，但**闸门不会替你猜**——读到自动化声明时只给 WARN 提示补声明。
**声明即承诺：两套判据不会同时满足，别想两头讨好。**

### 基准样本：no-bb 的 README 开场（老高亲写，这是要够到的标准）

```
# No BB —— 收敛约束中间层
> 在大模型和你的应用之间插一层，自动给每条对话拼上"想明白就收口"的约束。
> 不换模型 · 不训练权重 · 不截断思考过程 ——
> 实测思考量最多省 74%、响应时间最多快 47%，答案一道不错。

| 🧠 思考量 | 标准推理题 -51% · 长链高耗题 -33%（串行实测单题最高 -74%） |
| ⏱ 响应时间 | 串行实测墙钟最高 -47%（60.8s → 32.1s） |
| ✅ 正确率 | 42/42 → 42/42，零损伤（三组独立题集） |
| 🔌 接入成本 | 客户端改一行 base_url，模型名与 API key 不变 |
```

外加一段真实素材（把模型"哦，会不会是……不，不是……"的自我复述原文贴出来）——**让人看见问题本身**，而不是听你描述问题。

### 反面样本：selfopt 的开场（禁止这样写）

```
> 一个当前就能装进 AI 智能体用、能产生实测加速、且会随用户负载自我生长的小型优化框架。
## 三层分工
| Layer 1 候选生成 | LLM 智能体本身 | 读代码、识别可优化点、生成重写版本 |
## 数学闸门 v2.1 / Math Gate v2.1
- 等价性：随机 + 边界混合证人，异常语义比较…
- 性能：配对交替测量 + 中位数加速比 + 符号检验（sign test）…
```

问题：① 抽象名词开头，读者不知道装了干嘛；② 中英逐段对照，阅读双倍疲劳；③ 把实现原理当卖点。
**原理不是钩子，收益才是。**

### 两个自检（写完必做）

- **3 秒测试**：把 README 第一屏给一个不懂你这项目的人看 3 秒，问他"这东西能帮你什么"。答不上来 → 重写第 1、2 段。
- **转发测试**：你愿不愿意把它发到群里？如果第一反应是"还得补一句解释" → 不合格。

### 语言策略

- **GitHub**：英文为主（国际受众），但开头钩子可以保留中文版块。
- **SkillHub**：中文为主。
- **不要中英逐段对照**（selfopt 教训）。要双语就用 `README_EN.md` 分文件（no-bb 做法），主页保持单语干净。

---

## 三、给「大模型」看的东西：SKILL.md

### 铁律：这是**执行手册**，不是设计文档

大模型读它是为了**干活**，不是为了欣赏你的架构。

### 必写（缺一不可）

| 项 | 要求 |
|---|---|
| `description` | frontmatter 里必须有。一句话说清"干什么 + 什么时候用 + 触发词" |
| **触发词** | description 末尾列 5-8 个真实用户说法（no-bb 写法：触发词：no bb、收敛中间层、省思考 token…） |
| **开头第一段** | 一句话讲清是什么 + 核心价值 + 边界（格式如：`模型无关 · 零 key · fail-open · 响应零修改`） |
| **判定分支** | 多种走法时，先给"怎么选"的判据（no-bb：只问一件事——你的 agent 能不能注入文本？） |
| **命令表** | 常用操作一张表，能复制就走 |
| **装完验证** | 一条命令确认生效 |
| **适用边界** | 诚实写什么时候没用、什么场景别用（这条最容易被省，不许省） |

### 禁写（发现即删）

| 禁项 | 说明 |
|---|---|
| 开发日志 | "第 2 轮曾误认证 5 个…回滚才发现"、"历史教训：N_M_memory 的…" — 是 changelog，不是手册 |
| 数学证明过程 | sign test / p 值推导 / 零一完备性证明 — 放 `docs/`，别占 SKILL.md |
| 长段实证复述 | "实证：demo 场景1 的 join 重写在 `[]` 上…" — 结论一句话，过程删 |
| 原理先于用法 | 用法必须出现在文档前 1/3，selfopt 把用法塞到 50% 之后是反面教材 |

**判据：这一段内容，删掉后大模型还能不能正确干活？能删 = 该删。**

### 结构模板

```
---
name / slug / displayName / description（含触发词）/ allowed-tools
---

# 名字 — 一句话定位

<第一段：是什么 + 核心价值 + 边界，3 行内>

## 怎么选（若有多种走法）
<判据，最好一句话一个问题>

## 装/用
<最短路径命令>

## 常用操作
<表格>

## 装完验证
<一条命令>

## 适用边界
<什么时候没用 / 什么场景别用>

## 环境变量（若有）
<表格>
```

---

## 四、元数据：最容易糊弄、也最要命

**用户是先在搜索/列表页看到 description 和 topics，才决定点不点进来。**

| 项 | 硬要求 | 常见翻车 |
|---|---|---|
| **description** | 用户视角 + 收益 + 触发词，中文 ≤200 字 | 写实现机制（"hook 旁路注入 + 唯一入库闸门"）；或不写 |
| **topics** | 15~20 个，两组词都要有 | 空着；或只写技术分类词 |
| **触发词** | SkillHub 必填，5-8 个真实说法 | 不写；或写"技能/工具"这种废词 |
| **名称** | 中文、一眼懂、不带黑话 | "二创 tmeet 人员管理版"、"智能 py 脚本优化" |

### topics 两组词原则（老高 2026-09-06 定）

1. **技术分类词**：`ai` / `llm` / `python` / `proxy` / …
2. **效果作用词**：`token-optimization` / `latency` / `cost-optimization` / `test-time-compute` / …

**搜作用词的人才是被痛点扎到的目标用户。** 只写技术词的等于放弃这批人。

两组词齐全的示例（17 个 topics）：
`ai, chain-of-thought, context-window, cost-optimization, inference, latency, llm, llm-proxy, middleware, model-agnostic, prompt-engineering, proxy, python, reasoning, reasoning-tokens, test-time-compute, token-optimization`

selfopt 是反面（**0 个 topics**，而它下载量 201，纯靠 SkillHub 站内流量）。

---

## 四之二、划界：曝光与市场都不归流程管

> **流程只管可控的部分。不可控的东西写进流程，就等于给自己安了一个永远完成不了的指标。**

### 哪些不可控（明确划出去，不设指标、不追责）

| 不可控 | 为什么 | 流程里的处理 |
|---|---|---|
| **曝光量** | 有没有人来、平台推不推、站外有没有人转 | **不进流程**，不做引流动作要求 |
| **市场反应** | 下不下载、给不给 star | **不进质量校准**，只做参考 |

**推论：不发"引流动作清单"，不在 N-1 问"发在哪能被看见"。**
那是把不可控的东西伪装成可执行步骤——最后只会变成走过场。

**"不设指标" ≠ "不朝那个方向走"。**

曝光量不考核，是因为它不可控；但**方向必须一直朝着那边**——
**让搜到的人、路过的人觉得：这个东西可行，值得一试。**

**这句话才是发布物质量的真正目标。**
本文件里所有规则（前 30 行有收益陈述、topics 两组词、用法在前 1/3…）
**都只是这个目标的代理指标，不是目标本身。**

**判据倒过来的问法：**
> 不是"这份 README 符合几条规范"，
> 是"**一个路过的人，看完会不会想试一下？**"

符合全部规范但没人想试 = 不合格。
有个别规则没满足但一眼就想试 = 合格（规则该改，不是文档该改）。

### 哪些可控（这才是流程的全部职责）

| 可控 | 具体是什么 |
|---|---|
| **① 方向** | 该不该发、朝哪踢（N-1，老高答） |
| **② 让人觉得值得一试** | **← 质量的核心目标**，代理指标是给人看/给模型看/元数据三件套 |
| **③ 可发现性** | **想找的人找得到**（不是"让人看见"） |

**"可发现性"是唯一跟曝光有关、但归我们管的东西。**

### 可发现性 ≠ 曝光

| | 曝光（不可控） | 可发现性（可控） |
|---|---|---|
| 问的是 | 有多少人看见 | **想找的人能不能找到** |
| 归谁 | 平台 / 运气 / 渠道资源 | **发布物本身** |
| 具体落在 | — | `topics` / `description` / 名称：**写用户会搜的那个词** |

**写法要求**：topics 与 description 按"**用户会搜的词**"写，不按"描述得准确"写。

- ❌ `python-optimization-framework`（描述准确，但没人这么搜）
- ✅ `python 提速` / `脚本优化` / `token-optimization`（用户真的会敲进搜索框的词）

**GitHub 尤其如此**：它不带流量，topics + description 是**唯一能被人搜到的入口**，
写错就等于把东西埋了——这部分我们能控制，必须做对。

### 校准规则修正：数据只能证伪，不能证实

既然曝光不可控，**所有下载量数据都混着不可控的曝光噪声**，
没法从数据里分离出"发布物到底好不好"。

| | 能推出什么 |
|---|---|
| 下载高 | ❌ 不能推出"发布物好"（可能是流量红利、需求本身硬） |
| 下载低 | ❌ 不能推出"发布物差"（可能压根没人看见） |
| **明显差却下载高** | ✅ **能推出"标准可能漏了什么"**——这是唯一有效的用法 |

**所以：数据是提出疑问的，不是给出答案的。**
看到反常数据 → 人去查原因 → 人决定改不改标准。**没有自动校准这回事。**

---

## 四之三、N1 事实表：framing 登记（数据诚实的地基）

> **这是「拿 0 加速当加速卖」这道漏检的第一道闸。** 现有 N1 只登记「数字 + 出处」，没登记「这个数据该摆在哪、当什么用」。于是 92→0 这种真数字被写手自由摆成了「结果展示」主体，读者读出「这软件啥也优化不了」。

**事实表里的每个数字 / 结论，必须带一个 `framing` 字段，三选一：**

| framing 值 | 含义 | 在文档里的摆位硬约束 |
|---|---|---|
| `evidence`（正面证据） | 支撑「这东西能成」的真数字 | **必须前置**，出现在「它能做什么」章节 |
| `block`（拦截力佐证） | 负面结果，但用来证明「它替你挡了坑」 | **必须后置**于 evidence，且章节标题要写成「为什么需要它」，不得作为主体数据 |
| `bound`（边界说明） | 说明「什么时候没用 / 别误用」 | 出现在「适用边界」章节 |

**硬规则：**
1. 任何 `block` 类数据，**在 N2 文档里不得出现在第一屏、不得作为章节主体**。违反 = N2 准出一票否决。
2. 事实表若只登记了 `block` 类、没有任何 `evidence` 类 → **N1 直接打回**（说明这东西根本没正向价值，不发）。
3. `framing` 字段缺失的数字，N2 写手**无权使用**（脚本查 N2 文档引用的数字是否都在 N1 有 framing 登记）。

**这是 N1 签字的一部分，老高必须核：** 不只是「数字对不对」，更是「每个数字打算怎么摆」对不对。

---

## 五、发布前打分（两维度，各 5 分；任一维度 <3 分不许发）

### A. 用户视角：我有多想下载？（站在陌生人立场）

| 分 | 标准 |
|---|---|
| 5 | 3 秒看懂能帮我什么，有真实场景，想立刻试；且第 2 段到位（优化类给得出可复算数字 / 功能类说得出替你做了哪几件事） |
| 4 | 看懂了，但第 2 段没到位（优化类缺数字 / 功能类说不出具体替你做了什么）或钩子偏弱 |
| 3 | 知道是干什么的，但没打动我 |
| 2 | 看完要猜 |
| 1 | 只有功能罗列 / 术语堆砌 |

> **视角硬检（报告体检测）—— A 维度必做，不做等于未审**
>
> 大模型写手默认带着「自证代码正确」的思维写 README：平铺「我们测了 / 数据显示 / 事情就是这样 / 数据就是如此」，主语是「我们 / 数据」，不是「你 / 你的项目」。这种写法读者会在 0 结果处直接弃读，且写手自己和裁判同思维会互相背书、谁都发现不了。
>
> 裁判必须执行：
> 1. **主语统计**：列出「你 / 你的项目」句 vs 「我们测了 / 数据显示」句的占比；报告体句占比 **> 30% → A 维度直接 ≤ 2 分打回**。
> 2. **找 3 处报告体原文**（带行号），逐句改写成用户视角示范。
> 3. **负面数据 framing 检查**：「0 结果 / 负面数据」是否被写成卖点（拦截力 / 替你挡下），还是平铺成「啥也没成」的失败展示；后者判 ≤ 2 分。
> 4. **诚实误导检测（最关键，防「0 加速当加速卖」）**：数字本身可能全真、出处也可能全对，但写手把它摆在会让读者推出**假结论**的位置。裁判必须逐条核对：N1 事实表里标 `evidence` 的数字，在文档里是否出现在「它能做什么」章节（前置）；标 `block` 的数字是否出现在「为什么需要它」章节（后置）、且标题是「拦截力」而非「结果」。**真数字 + 错误摆位 = 误导 = 一票否决**，即使 5 分表能打 4 分。
> 5. **锚点强制**：以上每条必须给原文 + 行号，只给分数不给锚点 = 无效判定。
>
> 这一关防的是「写手自嗨报告体」+「诚实的误导」——它独立于上面的 5 分表，5 分表只看了「有没有收益陈述、3 秒懂不懂」，看不出视角对不对、也看不出真数字是否被摆歪。

### B. 大模型视角：我能不能照着干活？

| 分 | 标准 |
|---|---|
| 5 | description 带触发词，命令可复制，有判定分支 + 验证 + 边界 |
| 4 | 能干活，但部分信息要翻文档找 |
| 3 | 能干，但有多余内容干扰 |
| 2 | 原理淹没用法，需要读很久才找到怎么干 |
| 1 | 设计文档，无法执行 |

### C. 形态与元数据（一票否决项）

- [ ] **已声明 `kind:`**（`perf` 优化类 / `feature` 功能类）——决定第 2 段按哪套判据；不声明闸门只能给 WARN，不替你猜
- [ ] 平台形态选对（GitHub 项目 / SkillHub installer）
- [ ] 双平台同名可对应
- [ ] description 已写且是用户视角
- [ ] topics 已填且两组词齐全
- [ ] **每个 topic 都有人搜**（跑 `scripts/check_discoverability.py`；池子低于下限的换掉，别留只有自己的词）
- [ ] **名称查过重名**（同名且更热的竞品存在时，搜这个词的人先看到别人——此项只报不拦，改名代价高）
- [ ] SkillHub 侧触发词已写
- [ ] 版本号双平台数字一致

**任一项不过 → 不许发布。** 这比"发布后再改"便宜得多（发布过同版本不可重发，改一次要升版本号）。

### 两张清单别混：合不合规 vs 有没有人搜

| | 问的是 | 谁判 |
|---|---|---|
| `preflight_quality_check.py` | **有没有**（≥10 个 topics、含效果词、description 无黑话） | 本地，离线 |
| `check_discoverability.py` | **好不好用**（每个 topic 的真实搜索池、名称是否被占） | 要联网查 GitHub Search |

**前者过了不等于后者过。** 真实教训：20 个 topics 里 8 个池子 ≤3（`publish-pipeline` 全站只有自己 1 个），
`preflight_quality_check.py` 全 PASS —— 因为它量的是"有没有分两组"，不是"这词有没有人搜"。
