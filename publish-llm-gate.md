# publish-llm-gate.md — 大模型闸门（LLM as Gate）

> **脚本只能验证形式，只有大模型能验证语义。而流程走偏，偏的恰恰是语义。**

---

## 零、为什么状态机也管不住（先承认失败）

前面设计了状态机（`publish-flow-control.md`）：节点不能跳、产物必须落文件、exit code 控制推进。

**它能防跳步，防不了敷衍。**

因为每一步的"填空"仍然是 LLM 做的，而 LLM 可以**形式上满足全部校验**：

| 校验 | 敷衍怎么过 |
|---|---|
| `README.md` 存在 | 写一份套话模板，文件在 |
| 前 30 行有数字 | 塞一个 `"性能提升 10%"`（编的） |
| topics ≥10 | 填满 10 个技术分类词，没有效果作用词 |
| SKILL.md 有触发词 | 写"技能、工具、优化"这种废词 |
| 闸门 exit 0 | 全绿，流程继续 |

**脚本全绿，流程照样偏。** 不是状态机不够硬，是**它管的层面错了**——它管形式，偏差发生在语义。

### 更根本的漏洞

前面写的是"闸门在编排层（主 agent）手里，专家无权调用"。
**但主 agent 就是那个会偏的 LLM。** 把闸门交给主 agent = 交给被审者自己判定自己过关。

这个漏洞必须补。

---

## 一、解法：闸门本身就是大模型

**不是"用 LLM 执行流程"，是"用另一个独立的 LLM 判定流程有没有走对"。**

关键区别：

| | 执行者（主 agent） | 裁判（独立 LLM） |
|---|---|---|
| 目标 | **把事做完** | **找出没做对的地方** |
| 偏向 | 赶紧往下走 | 卡住不让过 |
| 产出 | 内容、文件 | 只有 `pass` / `fail` + 可验证理由 |
| 权限 | 读写项目 | **只读，无写权限** |
| 能否改 | 能 | **不能，只能打回** |

**为什么要 LLM 来当这个裁判？** 因为"这份 README 是不是针对这个项目写的"、"改的是不是上一轮被打回的那个问题"——这些脚本判不了，只能语义判断。

### 为什么裁判 LLM 比执行者可靠

不是因为它更聪明，是因为**任务性质不同**：

- **生成任务**容易偏（要创造，要取舍，会自洽化）
- **判断任务**更稳（只需比对，不须创造）

加上一条实证：LLM 评判**别人**的产出，明显比评判**自己**的产出准（self-bias / self-preference 问题）。
所以：**执行者不能评自己，这条铁律现在升级为——执行流程的人也不能判自己走得对不对。**

---

## 二、双裁判架构

| 层 | 谁 | 判什么 | 判不了什么 |
|---|---|---|---|
| **硬闸门** | 脚本 | 有没有、格式对不对、exit code | 好不好 |
| **流程裁判**（新增） | 独立 LLM | **这一步真的做了吗？改的是不是被打回的那个问题？** | — |
| **质量裁判** | 独立 LLM（即原"挑刺用户"） | 用户会不会下载 / 大模型能不能照着干 | — |

**三者全过才推进。** 脚本是必要条件，裁判是充分条件。

### 流程裁判 vs 质量裁判

- **质量裁判**看**产物好不好**（评判对象 = 发布物）
- **流程裁判**看**执行对不对**（评判对象 = 执行者的行为）

流程裁判是这次新增的，之前完全没有。

---

## 三、流程裁判的四问（每一步结束必答）

这是让它真正有用的东西。裁判必须**逐条书面回答**，不能只给结论：

| # | 问 | 防什么 |
|---|---|---|
| **1** | **这份产物是不是针对这个项目写的？** 有没有可复用到任何项目的通用套话？ | 防模板敷衍 |
| **2** | **产物里的事实（数字、定位句）能不能追溯到 fact-sheet？** 有没有事实表之外的新数字？ | 防编数字 |
| **3** | **如果这一轮是"打回后修改"——改的是不是上一轮被打回的那个点？** 有没有改了别处交差？ | **防假修改（最常见）** |
| **4** | **有没有做没让它做的事？**（顺手改代码、顺手"优化"、扩权） | 防越权 |

**第 3 问是最关键的。** 打回后敷衍修改，是流程失效最常见的形态：
脚本看到"文件更新了"，状态机看到"重新提交了"，但改的压根不是被指出的那个问题。

### 裁判必须能看历史

给它 `.publish-staging/history/` 里上一版产物 + 上一轮打回理由。
**没有对比就没有第 3 问。**

---

## 四、让裁判不失效的三个条件

裁判也是 LLM，也可能变成"照本宣科的 checklist 执行者"。防止它失效：

### ① 给立场，不给清单

- ❌ "检查 README 是否符合规范第 1-4 条" → 变成勾选机器
- ✅ "你是一个完全不懂这项目的人，读完告诉我你装不装" → 有立场才会真判断

### ② 给锚点（好样本 + 坏样本）

让裁判**同时看**两份真实材料：
- 好：`everytime-novel`（SkillHub 1066 下载，简介最完整）
- 坏：`selfopt`（数学公式开篇，topics 为 0）

**有了对比，标准才具体。** 光给规则，LLM 会自己脑补标准松紧。

### ③ 判语义，不判形式

- ❌ "description 是否 ≥50 字"
- ✅ "这句话是在说用户能得到什么，还是在说内部怎么实现的"

形式判断交给脚本，语义判断才交给裁判。**别让裁判干脚本的活，那是在浪费它。**

> ⚠️ **2026-09-12 补：还有第三类判断，裁判也别干——"代码是不是真的"。**
>
> | 谁 | 管什么 | 判据 |
> |---|---|---|
> | **脚本** | 形式 | 有没有、格式对不对 |
> | **裁判** | 表达 | 用户/大模型看不看得懂、会不会想用 |
> | **审计**（`publish-code-audit.md`） | **真实** | 需求有没有真实现、超纲必要与否、文档里的东西**实跑存不存在** |
>
> **别让裁判去审代码真假。** 裁判看的是产物（README/SKILL.md），
> 而"文档里写的 `pip install x` 实际不存在"这类问题，裁判读文本读不出来，**必须实跑**。
> 那是审计代理的活，工具也不同（它给只读 Bash 白名单）。
>
> 反过来也成立：**审计不评审表达。** 它只说"这条有没有锚点"，不说"这句吸不吸引人"。

### ④ 判定必须可验证

`fail` 必须附**可验证理由**：引用具体行号、引用具体缺失项。

> ❌ "README 不够吸引人"
> ✅ "第 3-11 行讲的是三层架构，读者看完不知道装了能省什么；fact-sheet 里的 `-51%` 没有出现在前 30 行"

**理由可验证 = 判定可被推翻 = 可被审计。** 这条防的是"裁判瞎卡"。

### ⑤ 只给它真实用户会看的那部分（防信息污染）

**这是最容易被忽略、也是实证过的一条。**

selfopt 那次的事故已经证明：**立场会被信息污染。**
写手读了 sign test 的实现之后，真心觉得那是最好的开场白——它的"用户视角"在读源码那一刻就没了。

挑刺专家同理：**读了完整 README，它就不再是不懂的人了。**

| 做法 | 结果 |
|---|---|
| ❌ 把完整 README 给"陌生人"专家 | 它读完全文已经被说服，立场失效 |
| ✅ **只给前 30 行**（真实用户在 GitHub 上就只看首屏） | 立场保真 |

**规则：给裁判的输入，必须等于真实用户在该场景下的实际输入。**
真实用户看首屏，裁判就看首屏；真实用户看列表页的 description，裁判就看 description。

多余的信息不是"让它判断更准"，是**把它同化**。

### ⑥ 裁判的身份：平台定读者池，事实表定池里的谁（2026-09-12 立；2026-09-23 补第一级）

**事故经过：**

selfopt 挑刺时，N1 事实表写明目标用户是「写 Python 的 **AI 智能体**开发者」，
但拉起挑刺用户时设定成了「写 Python 的开发者」——**漏掉了"AI 智能体"这个修饰词**。

结果它给出的核心批评是：**「收益是二阶的，变强的是我的 agent 不是我」**，打 **2 分**。

**这条批评无效**（老高当场指出）：给 agent 用的库，"让 agent 变强"**就是直接收益**。
不是产品有问题，是**评价者找错了人**。

改成「正在开发 AI 智能体的开发者」重评 → **4 分通过**。
而且正确身份的评价者立刻指出了真正的价值点：
**「勾住我的是拦住错误重写和跨会话记忆，不是那点加速」**——
这个洞察，身份错配的那位根本给不出来。

**规则（硬）：**

1. **先由平台定读者池**（`publish-quality.md` §一）：
   GitHub 侧 = 国际开发者（英文优先）；SkillHub 侧 = 中文用户（中文优先）。
   裁判身份**不是**"事实表目标用户"的同义词 —— 它是**该平台上的真实访客**。
2. **再用事实表把读者池精确化**：身份设定**不得由主 agent 自行决定**，
   必须**原样取 N1 事实表的「目标用户」字段**，**一个修饰词都不许丢**。
   —— selfopt 那次丢的是"AI 智能体"这个修饰词，**平台没变**。
   所以这条拦的是"**丢修饰词**"，不是"**按平台分身份**"。
3. **判据**：如果一条批评能被「**但你不是这个平台上的目标读者**」驳回 → **身份错配，该判定整份作废重跑**。
4. 作废的判定要留档（记进 `verdict-*.md` 并注明作废原因）——**防同一个坑踩第二次**。

> **两条的关系（2026-09-23 澄清，老高定）**：
> **平台管"在哪个读者池里评"，事实表管"这个池里的谁"。二者不冲突，是两级。**
>
> **反例（本次实跑，已更正）**：everytime-novel 是纯中文产品（目标用户 = 中文网文作者），发到 GitHub。
> 曾把 J2（国际开发者身份）的 2 分按旧措辞判为"身份错配、作废"—— **这是错的**：
> GitHub 上的真实访客确实包含国际开发者，J2 的身份没错，那条批评**有效**。
> 而且它当场产出了真东西：README 顶部补了一段 ~90 词英文摘要。
>
> **推论**：发到 GitHub 的中文项目，J2 大概率会扣分 —— 那不是误报，是**真实缺口**。
> 处理方式见 `publish-quality.md` §一（"英文优先"）；**是"必须补英文镜像"还是"声明即通过"，见名册 §六 Q10**。

**与 ⑤ 的关系**：⑤ 管"给多少信息"，⑥ 管"是谁在读"。
**信息对了但人不对，一样产出无效批评——而无效批评会污染修改方向，比没有批评更糟。**

### ⑦ 允许推翻标准，但记录

裁判如果认为规范本身有问题（比如"这条规则在中文平台反而伤"），
允许它 `fail` 并注明"**规则存疑**"——但**不许它自己改规则**，只记录，交人裁决。

---

## 五、裁判怎么不被执行者带偏

| 手法 | 说明 |
|---|---|
| **不听陈述** | 裁判**只看产物文件**，不看执行者的"我做完了，因为……"。执行者最容易骗的就是通过叙述 |
| **独立判据** | 依据只有两个：fact-sheet + 规范文档。不听执行者解释规范 |
| **无写权限** | 只读子 agent，物理上改不了任何东西 |
| **判定落文件** | `.publish-staging/verdict-<节点>.md`，进状态机作为推进依据 |
| **不能自评** | 裁判不评判自己的判定 |

---

## 六、成本：不是每步都叫裁判

裁判贵（多一轮 LLM）。按节点风险分级：

| 节点 | 硬闸门 | 流程裁判 | 质量裁判 |
|---|---|---|---|
| N0 形态判定 | ✅ | — | — |
| N1 事实表 | ✅ | — | —（**人签字**） |
| N2-G / N2-S 写发布物 | ✅ | ✅ | ✅ |
| N3-X 交叉校验 | ✅ | — | — |
| N4 挑刺 | — | — | ✅（这就是质量裁判） |
| N5 装配 | ✅（git diff） | ✅ | — |
| N6 发布 | ✅ | — | — |
| N7 收口 | ✅（三行契约） | ✅（**最后一次总查**） | — |

**重裁判放在 N2（写内容）和 N7（收口前总查）**——前者是偏差源头，后者是最后一道。

小版本（改 bug 升号、内容未变）可只跑硬闸门 + N7 流程裁判。

---

## 六之二、运行时形态：三权分立

### 一句话答案

**派发子代理；裁判常驻不重建；传递归主 agent，推进归脚本，判定归裁判。**

**闸门代理不做传递。** 因为"传递权"就是"决定流程往下走"的权力——
谁传递，谁就同时握住了判定和推进，又变回自己判自己。

### 三权怎么切

| 权 | 归谁 | 具体动作 | 它**没有**的权力 |
|---|---|---|---|
| **判定** | 裁判子 agent（**常驻**） | 读产物 → 写 `verdict-<节点>.md`（pass/fail + 可验证理由） | 不能推进、不能改文件 |
| **推进** | 脚本 `publish_flow.py` | 校验 gate log + verdict → 改 `.publish-state.json` | 无裁量，exit code 说话 |
| **传递** | 主 agent（**纯调度**） | 调 `next` → 拉执行子 agent → 调 `done` → 把暂存区路径交给下一步 | **不判质量、不写内容、不改推进结果** |

**三者互不包含**：裁判无权推进，推进者无裁量，传递者无判断。

### 主 agent 要被削到什么程度

| 工具 | 给不给 | 理由 |
|---|---|---|
| `Bash`（限 `publish_flow.py`） | ✅ | 只跑状态机命令 |
| `Agent`（拉子 agent） | ✅ | 只做派发 |
| `Read`（项目源码 / 产物） | ❌ | 读了就会"顺便理解"，理解就会"顺手改" |
| `Write` / `Edit` | ❌ | **想顺手改都做不到** |

**连 N1 事实表的起草都派子 agent**（研究型：Read 源码 + 写暂存区），
主 agent 全程不碰内容——它只认 exit code 和脚本输出。

### 为什么裁判要常驻、不每步重建

流程裁判的**第 3 问**（"改的是不是上一轮被打回的那个点"）**依赖记忆**。
每步新建裁判 = 每次失忆 = 第 3 问必然失效。

**"常驻"的落地形态（2026-09-23 定，此前只写了"要常驻"没写"怎么常驻"）：**

子 agent 是无状态的，"常驻"必须落到具体动作——
**同一个裁判 agent 用续跑消息接着跑**，每轮只喂三样：
① 判定摘要（不喂历史全文）② 当前产物 ③ 上轮打回理由。

常驻的两个约束：
- **只存判定摘要，不存全文**——防上下文膨胀后判断力下降
- **只读产物，不听执行者陈述**——执行者最容易通过叙述骗过裁判

> 同源约束（2026-09-23 定）：**J1 流程裁判与 E1 写手必须不同模型**。
> 裁判与执行者同源会共享盲区——selfopt 那次"4 分放行"就是同源互背书的结果。

### 产物走文件系统，不走主 agent 的嘴

这是蜂巢洞察在本场景的应用：
**执行子 agent 的产物直接落 `.publish-staging/`，裁判直接读文件；
裁判的判定直接落 `verdict-*.md`，脚本直接读文件。**

主 agent **不转述任何一方**。转述就是失真——
执行者写的东西经主 agent 一转述，偏差就进了裁判的输入；
裁判的判定经主 agent 一转述，"不行"就变成"需要优化"。

### 一个节点的完整调用序列

```
1. 主 agent → publish_flow.py next
              拿到：任务卡（要产出什么 / 准入条件 / 约束）
2. 主 agent → 拉【执行子 agent】
              输入：暂存区路径 + 事实表路径 + 规范文档路径
              输出：产物文件（不经过主 agent）
3. 主 agent → publish_flow.py verify N2-S
              脚本查形式 → gate-s.log（exit code）
4. 主 agent → 调【裁判子 agent（常驻）】
              输入：产物路径 + 历史路径 + 上轮打回理由
              输出：verdict-N2-S.md（不经主 agent 转述）
5. 主 agent → publish_flow.py done N2-S
              脚本读 gate-s.log + verdict → 全 pass 才推进
              fail → 打回，最多 3 轮，超限**自动不发**（deferred，留报告）
```

**主 agent 在第 2~4 步之间不做任何判断**，只是按顺序发起调用。

---

## 七、诚实边界

| 防不住 | 说明 |
|---|---|
| **裁判与执行者同源** | 同一个模型、同一个系统提示 → 可能犯同样的盲区。缓解：裁判用**不同模型**或至少不同温度/角色设定 |
| **裁判被冗长产物淹没** | 产物太长时 LLM  judgement 质量下降。缓解：裁判只看关键片段，不读全文 |
| **裁判瞎卡** | 过度严格导致流程卡死。缓解：理由可验证 + 3 轮上限 + 超限后**自动不发**（不阻塞，留报告） |
| **合谋** | 理论上两个 LLM 互相放行。缓解：**脚本层独立**（脚本不会被说服），且最终人签字在 N1 |

**结论：大模型闸门把"敷衍过关"从"必然"降到"需要骗过另一个独立的脑子"。**
它不完美，但它是目前唯一能管语义层的机制——脚本在那儿无能为力。

---

## 八、无人值守模式（2026-09-12 定）

人在场时，质量靠"事后发现判错了再改"。人一旦不在场，这条路就断了，
只能靠**事前就不可能放过** + **默认安全位**顶上。

三重锁：

| 锁 | 做法 | 防的是 |
|---|---|---|
| **判据硬化** | 能机器判的全部下沉到脚本 | 减少"需要判断"的总量 |
| **分级全票**（2026-09-23 修订） | **P0（凭据/幻觉/版本打架/装不上）→ 全票**，任一 fail 即打回；**P1（文案吸引力/表达）→ 多数票**，分数下限按同类产物历史中位数校准 | 单点放飞 + 通过率过低 |
| **默认安全** | 重试到上限仍不过 → **自动不发** | 错误扩散 |

> **为什么从"全票"改成"分级全票"**：原设计 4 个判定者任一 fail 即打回、3 轮上限。
> 若单个裁判通过率 0.8，全过率 = 0.8⁴ = **0.41**，3 轮内至少过一次仅 79%——
> **约五分之一的东西会被自动搁置**。原文写"代价是些微漏发"，实测不些微。
> 分级后 P0 保持零容忍，P1 用多数票 + 历史中位数校准兜住通过率。

**两个终态，只有两个**：

| 终态 | 含义 | 人的动作 |
|---|---|---|
| `released` | 已发布 | 收到一条通知，**无需操作** |
| `deferred` | 已搁置（默认安全位） | **无**。报告留在暂存区，想看再翻 |

**"不用操心"的准确含义**：不是"系统替你做决定然后瞒着你"，
是"**决定可以永远不做，而且默认可推迟**"。

所以第五节那张表里的"打回超 3 轮 → 交人"被替换掉了——它会扣住人。
改成超限不发之后，人从"每次必须看"变成"可以永远不看"。
代价是些微漏发（假阴性换真阴性），这是全自动必付的交换。

### 判决账本

每次判定落一行 `.publish-staging/ledger.jsonl`：
时间 / 节点 / 轮次 / 结论 / **分数** / 分数下限 / **判定者署名** / 判据 / 产物。

**为什么必须有分数**：pass/fail 是二值的，"过了就是过了"，
放宽和合格长得一模一样。有分数才能发现"同类产物这次 2 分、上次 4 分"的**漂移**——
那是放宽标准唯一能被抓到的形态。

### 产物落在哪（`need_at`）

节点表里每个节点标了产物落点，两边必须一致——这是 2026-09-12 实测踩到的坑：

| 落点 | 用于 | 例 |
|---|---|---|
| `staging`（默认） | 中间产物，暂存区 `.publish-staging/` | `form.json`、`fact-sheet.md` |
| `project` | **发布物本身**，覆盖项目里的同名文件 | `README.md` |

坑在哪：N2 把 README 写进暂存区，N3 硬闸门查的却是项目根 → **闸门永远看不到产物**，
一路判 fail 直到搁置。发布物就该落在项目根，N3 查的也是那里。

### 判定收件箱（语义节点的唯一入口）

机器判不了的节点（N4 质量裁判 / N5 装配 / N6 发布 / N7 收口）不猜、不放行，
只读一个收件箱：

```
<项目>/.publish-staging/verdicts/<节点>.json
    {"score": 0-5, "judge": "谁判的", "reason": "可验证的判据"}
```

三条规矩：

1. **执行器只读不写。** 判定必须由执行器之外的主体投递——主 agent 派出的只读子 agent，
   或人。执行器自己写 = 自评，闸门作废。
2. **收件箱空着 = 判 fail。** 默认安全位，绝不因为"没判"而放行。
3. **有分数下限的节点，判定必须给分。** 缺分数一样判 fail。

这样"全自动"和"有人负责"不打架：能机器判的全自动跑，
需要判断的必须有人（或子 agent）签字，签不了就不发。

### 谁来判：判定派发（出题 / 收卷）

收件箱解决了"判定从哪读"，还差"该判了"这个信号——没人提醒，链子会静静地停在原地。
`scripts/verdict_dispatch.py` 补的就是这一环，它自己不判、也不碰收件箱：

| 动作 | 干什么 |
|---|---|
| `brief` | 按当前节点生成**判定任务单** → `.publish-staging/pending/<节点>.task.json`。写明读哪些材料、按什么标准打分、回填到哪、回填什么格式 |
| `collect` | 扫描 `verdicts/`，校验每份判定是否合法、会不会通过。**只校验不改写** |
| `status` | 报当前节点由谁判（机器还是外部） |

**三权边界，合起来才成立**：

```
local_executor.py     只读 verdicts/   → 执行者不自评
verdict_dispatch.py   只写 pending/    → 派发者不代判
判定者（只读子 agent / 人）             → 唯一有权写 verdicts/ 的主体
```

任务单**不含"我觉得这步做得怎么样"的任何判断**，只给材料与标准；
判定者必须自己去读产物，给出带锚点（文件:行号）的判据。
若某节点既无材料也无取证据命令，出题直接失败——**不出零证据的题**，
因为那种任务单只能换来一个盖章。

### 停下等投递（`--pause-external`）

无人值守 + 派发器的工作位。走到需外部判定的节点、而收件箱里还没有判定时：

```
停下，退出码 10，打印任务单位置  ——  不消耗重试次数、不落账本
```

**"还没人判"不等于"判 fail"。** 没人判就一路判死搁置，等于把还没来得及判的东西当垃圾扔掉。
`local_publish.py` 把这条包成单入口：反复调它，退出码 10 就派人判、0 就到终态。

**判定无效 ≠ 判定否决**，两者的处置相反：

| 情形 | 例子 | 处置 |
|---|---|---|
| **判定无效** | 缺署名、缺判据 | **原地退回**，不消耗重试——这是"写错了" |
| **判定否决** | 分数低于下限、判据指向真问题 | 吃重试次数，连续 3 轮不过 → `deferred`——这是"做不到" |

混为一谈的后果：一份忘了署名的判定会被连读三次 → 直接判死搁置，
把"手滑"当成"不合格"。

### 配套脚本

| 脚本 | 职责 | 自检 |
|---|---|---|
| `scripts/local_publish.py` | **单入口**：一条命令反复调；退出码 0=终态 / 10=等投递 / 1=错误 | 15 项 |
| `scripts/publish_flow.py` | 状态机 + 判决账本（推进权） | 23 项 |
| `scripts/local_executor.py` | **本机真实执行器**：机器节点真跑脚本，语义节点读收件箱 | 18 项 |
| `scripts/verdict_dispatch.py` | 判定派发：出题（任务单）+ 收卷（校验合法性） | 25 项 |
| `scripts/detect_publish_form.sh` | N0 形态判定（skill / installer / github-project） | 5 项 |
| `scripts/quality_metrics.py` | 客观代理指标（可机器算的那半边），对 README / SKILL.md 打分 | 6 项 |
| `scripts/executor_example.py` | 外部执行器接入样板（教怎么换成自己的执行器） | — |

每个都自带 `--test`，一条命令可自证；合计 92 项。

### 怎么跑一次

```bash
python3 scripts/publish_flow.py --project <项目> init
python3 scripts/publish_flow.py --project <项目> run --auto \
    --exec "python3 scripts/local_executor.py"
python3 scripts/publish_flow.py --project <项目> status
```

